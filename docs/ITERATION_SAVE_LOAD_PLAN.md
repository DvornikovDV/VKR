# Итерация: Сохранение и загрузка схем с хранением изображений в Base64

**Версия**: 1.0  
**Дата создания**: 30.01.2026  
**Статус**: В планировании

---

## Обзор

Реализация функционала сохранения и загрузки схем редактора с полным хранением изображений в формате Base64 внутри JSON файлов.

**Цель**: Позволить пользователям сохранять полные схемы (изображения + структура + соединения + виджеты) в одном JSON файле и загружать их обратно в редактор без потери данных.

---

## Архитектура решения

### Вариант 1: JSON + Base64 (текущий выбор)

- ✅ Сохранение изображений как Base64-строк внутри JSON
- ✅ Полностью самодостаточный JSON файл
- ✅ Не требует серверной части на текущем этапе
- ✅ Готово для демонстрации и тестирования
- ⚠️ Размер файла на 33% больше исходного
- ⚠️ Медленнее для больших изображений (>5MB)

### Структура JSON

```json
{
  "version": "1.0",
  "timestamp": "2026-01-30T12:27:00Z",
  "images": [
    {
      "id": "img_1",
      "name": "schema_element.png",
      "width": 800,
      "height": 600,
      "data": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAY...",
      "x": 100,
      "y": 50,
      "scaleX": 1,
      "scaleY": 1
    }
  ],
  "connectionPoints": [...],
  "connections": [...],
  "widgets": [...]
}
```

---

## Основные задачи

### Фаза 1: Обновление ImageManager (~ 2 часа)

1. **Проверка текущей реализации**
   - Проверить `public/file-manager.js`
   - Убедиться, что кнопки "Сохранить" и "Загрузить" работают
   - Оценить текущий экспорт/импорт изображений

2. **Добавление Base64 конвертации**
   - Метод `imageToBase64(konvaImage)` - конвертация в Base64
   - Метод `exportImages()` - возвращает array с Base64 данными
   - Обновить метод `importImages()` - загрузка из Base64

3. **Интеграция с FileManager**
   - Сделать `exportImages()` асинхронным
   - Обновить JSON структуру в `saveScheme()`
   - Обновить парсинг в `loadScheme()`

### Фаза 2: Обновление FileManager (~ 1 час)

1. **Актуализация saveScheme()**
   - Добавить `await imageManager.exportImages()`
   - Включить Base64 данные в JSON

2. **Актуализация loadScheme()**
   - Парсить JSON с Base64
   - Передать ImageManager для восстановления

3. **Обработка ошибок**
   - Try-catch для ошибок парсинга JSON
   - Валидация структуры файла
   - Информативные сообщения об ошибках

### Фаза 3: Тестирование (~ 1.5 часа)

1. **Функциональное тестирование**
   - Сохранить схему с изображениями
   - Загрузить сохраненную схему
   - Проверить целостность изображений
   - Проверить положение и масштаб

2. **Граничные случаи**
   - Пустая схема (без изображений)
   - Множество изображений (10+)
   - Большие изображения (>5MB)
   - Поврежденный JSON

3. **UI/UX тестирование**
   - Кнопки "Сохранить" / "Загрузить" в панели инструментов
   - Сообщения об успехе/ошибке
   - Прогресс сохранения для больших файлов

---

## Технические детали

### Ключевые методы

#### ImageManager

```javascript
// Новый метод: конвертация одного изображения
imageToBase64(konvaImage) {
    return konvaImage.toDataURL();
}

// Обновленный метод: экспорт с Base64
async exportImages() {
    return Promise.all(this.images.map(async (img) => ({
        id: img.id,
        name: img.imageName || 'image.png',
        data: this.imageToBase64(img.image),
        width: img.width(),
        height: img.height(),
        x: img.x(),
        y: img.y(),
        scaleX: img.scaleX(),
        scaleY: img.scaleY()
    })));
}

// Обновленный метод: импорт из Base64
importImages(imagesData) {
    imagesData.forEach(data => {
        const img = new Image();
        img.onload = () => {
            this.addImage(img, data.x, data.y, data.id);
        };
        img.src = data.data;  // Base64!
    });
}
```

#### FileManager

```javascript
// Обновленный метод: асинхронное сохранение
async saveScheme() {
    try {
        const scheme = {
            version: '1.0',
            timestamp: new Date().toISOString(),
            images: await this.imageManager.exportImages(),
            connectionPoints: this.connectionPointManager.exportPoints(),
            connections: this.connectionManager.exportConnections(),
            widgets: this.widgetManager?.exportWidgets() || []
        };
        
        this.downloadJSON(JSON.stringify(scheme, null, 2), 'scheme.json');
        alert('Схема успешно сохранена!');
    } catch(e) {
        console.error('Ошибка при сохранении:', e);
        alert('Ошибка при сохранении схемы');
    }
}

// Обновленный метод: загрузка с восстановлением изображений
loadScheme(file) {
    const reader = new FileReader();
    reader.onload = (e) => {
        try {
            const scheme = JSON.parse(e.target.result);
            
            // Очистить редактор
            this.clearAll();
            
            // Восстановить все компоненты
            this.imageManager.importImages(scheme.images || []);
            this.connectionPointManager.importPoints(scheme.connectionPoints || []);
            this.connectionManager.importConnections(scheme.connections || []);
            this.widgetManager?.importWidgets(scheme.widgets || []);
            
            alert('Схема успешно загружена!');
        } catch(e) {
            console.error('Ошибка при загрузке:', e);
            alert('Ошибка при загрузке схемы: ' + e.message);
        }
    };
    reader.readAsText(file);
}
```

---

## Критерии завершения

### ✅ Обязательно

- [ ] ImageManager поддерживает Base64 экспорт/импорт
- [ ] FileManager асинхронно работает с Base64
- [ ] Сохраненный JSON содержит полные Base64 строки изображений
- [ ] Загруженная схема восстанавливает изображения без потери качества
- [ ] Кнопки "Сохранить" и "Загрузить" в UI работают корректно
- [ ] Нет ошибок в консоли браузера

### ⚠️ Желательно

- [ ] Поддержка версионирования схем (версия в JSON)
- [ ] Валидация JSON структуры при загрузке
- [ ] Информативные сообщения об ошибках
- [ ] Поддержка миграции старых форматов (если есть)

### 📊 Метрики

- Время сохранения: < 2 сек (для 10MB схемы)
- Размер JSON: исходный × 1.33 (Base64 overhead)
- Успешное восстановление: 100% изображений

---

## Зависимости

### Текущие

- `public/file-manager.js` - основной класс
- `public/image-manager.js` - управление изображениями
- `public/connection-point-manager.js` - точки соединения
- `public/connection-manager.js` - соединения
- `public/index.html` - UI с кнопками сохранения

### Внешние

- Konva.js (`toDataURL()` для экспорта изображений)
- Встроенный `Image API` браузера

---

## График реализации

| Фаза | Задачи | Время | Статус |
|------|--------|-------|--------|
| 1 | ImageManager + Base64 | 2ч | 🔴 Не начато |
| 2 | FileManager обновление | 1ч | 🔴 Не начато |
| 3 | Тестирование | 1.5ч | 🔴 Не начато |
| **Итого** | | **4.5ч** | 🔴 Не начато |

---

## Риски и ограничения

### Риски

1. **Размер файла** - Base64 увеличит размер на 33%
   - Решение: документировать ограничение, рекомендовать <10MB

2. **Производительность** - медленное кодирование больших файлов
   - Решение: показывать прогресс, использовать асинхронность

3. **Комплексность** - много асинхронных операций
   - Решение: использовать Promise.all для параллельной обработки

### Ограничения текущие

- ❌ Нет серверной части для облачного хранилища
- ❌ Нет версионирования (перезапись файла)
- ❌ Нет сжатия JSON (можно добавить позже)

### Будущие улучшения (Фаза 2)

- IndexedDB для локального хранилища
- Облачное хранилище на сервере
- Версионирование и история
- Сжатие JSON (gzip)

---

## Ссылки

- **GitHub Branch**: `dev/save_load`
- **Related Issues**: #TODO
- **Documentation**: `docs/BASE64_EXPLANATION.md`, `docs/BASE64_QUICK_REFERENCE.md`

---

## Авторы и история

- **Создано**: 30.01.2026
- **Автор**: Development Team
- **История версий**: 
  - v1.0 (30.01.2026) - Начальный план
