# 🗺️ КАРТА ФАЙЛОВ ПРОЕКТА

**Версия**: 1.0  
**Дата обновления**: 03.01.2026  
**Статус**: ✅ Актуальная после рефакторинга

---

## 📂 СТРУКТУРА ПРОЕКТА

```
VKR/
├── public/                           🎯 ОСНОВНОЙ КОД
│   ├── index.html                    (точка входа браузера)
│   ├── main.js                       (инициализация приложения)
│   ├── canvas-manager.js             (управление Konva.js)
│   ├── ui-controller.js              (координатор менеджеров)
│   ├── diagram-element.js            (базовый класс элементов)
│   ├── image-manager.js              (работа с изображениями)
│   ├── connection-point-manager.js   (точки соединения)
│   ├── connection-manager.js         (линии соединений)
│   ├── selection-manager.js          (выделение элементов)
│   ├── properties-panel.js           (панель свойств)
│   └── file-manager.js               (сохранение/загрузка)
│
├── doc/                              📚 ДОКУМЕНТАЦИЯ
│   ├── vision.md                     (техническое видение)
│   ├── conventions.md                (соглашения кодирования)
│   └── workflow.md                   (процесс разработки)
│
├── refactoring-strategy.md           (план рефакторинга)
├── REFACTORING_COMPLETE.md           (отчет о завершении)
├── FILE_MAP.md                       (этот файл)
└── tasklist.md                       (список задач)
```

---

## 📋 ДЕТАЛЬНАЯ КАРТА ФАЙЛОВ

### 🌍 ТОЧКИ ВХОДА

#### `index.html`
**Что?** Главный HTML файл браузера  
**Когда менять?** Если нужно добавить новые кнопки, поля ввода или изменить разметку  
**Хранит?** HTML-структура страницы, ID элементов UI, подключение Konva и других библиотек  
**Зависимости?** → Konva.js, Bootstrap 5, CSS (если есть)

**Элементы для поиска:**
- `#add-image-btn` - кнопка добавления изображения
- `#save-btn` - сохранение схемы
- `#load-btn` - загрузка схемы
- `#clear-btn` - очистка canvas
- `#create-line-btn` - режим создания линий
- `#container` - контейнер для Konva stage
- `#properties-content` - панель свойств
- `#file-input` - скрытый инпут для файлов

---

### 🚀 ИНИЦИАЛИЗАЦИЯ

#### `main.js`
**Что?** Точка входа приложения  
**Размер?** 10 строк (стабильный!)  
**Когда менять?** НИКОГДА - должна оставаться простой  
**Хранит?** Импорт UIController, инициализация при DOMContentLoaded  

```javascript
import { UIController } from './ui-controller.js';

document.addEventListener('DOMContentLoaded', () => {
    new UIController();
});
```

---

### 🎮 ЯДРО ПРИЛОЖЕНИЯ

#### `ui-controller.js` ⭐ КООРДИНАТОР
**Что?** Главный контроллер, медиатор между менеджерами  
**Размер?** ~450 строк (было 1500!)  
**Когда менять?** При изменении логики взаимодействия менеджеров или UI-событий  
**Хранит?**
- Инициализация всех менеджеров
- Настройка callbacks между менеджерами
- Обработчики UI-событий (клики кнопок)
- Логика режима создания линий
- Вспомогательные методы (getPointerCoords, getNearestSide)

**Методы:**
```javascript
// Основные
init()                              // инициализирует все менеджеры
setupManagerCallbacks()             // связывает менеджеры
setupEventListeners()               // подключает UI-события

// Режим создания линий
toggleLineCreationMode()            // включить/выключить режим
setupLineCreationMode()             // подготовка режима
teardownLineCreationMode()          // выключение режима
handlePinClickForLineCreation()     // клик по пину в режиме
handleMouseMoveForLinePreview()     // движение мыши для предпросмотра

// Помощники
getPointerStageCoords()             // позиция курсора на canvas
getNearestSideAndOffsetFromFrame()  // ближайшая сторона рамки
updatePreviewLine()                 // обновление предварительной линии
clearPreviewLine()                  // очистка предварительной линии
```

**Менеджеры, которые создает:**
```javascript
this.canvasManager                  // управление canvas (передается из вне)
this.imageManager                   // добавление изображений
this.connectionPointManager         // точки соединения
this.connectionManager              // линии соединений
this.selectionManager               // выделение
this.propertiesPanel                // панель свойств
this.fileManager                    // файлы
```

**Когда искать код:**
- Нужно добавить новую кнопку UI? → `setupEventListeners()`
- Нужно поменять связь между менеджерами? → `setupManagerCallbacks()`
- Нужно изменить логику режима линий? → `toggleLineCreationMode()` и друзья

---

#### `canvas-manager.js` ✅ СУЩЕСТВУЕТ
**Что?** Управление Konva stage и layer  
**Статус?** Уже готов, не менять без необходимости  
**Хранит?**
- Инициализация Konva stage
- Методы getStage(), getLayer()
- Добавление сетки (addGrid)
- Pan/zoom логика

**Методы:**
```javascript
getStage()          // получить Konva stage
getLayer()          // получить Konva layer
addGrid()           // нарисовать сетку
// Остальное смотри в самом файле
```

---

### 📦 МЕНЕДЖЕРЫ

#### `diagram-element.js` (БАЗОВЫЙ КЛАСС)
**Что?** Базовый класс для всех элементов диаграммы  
**Размер?** 60 строк  
**Когда менять?** Если нужна новая общая функциональность для всех элементов  
**Хранит?** Общие методы для работы с метаданными и Konva-объектами  

**Методы:**
```javascript
getShape()              // получить Konva объект
setMeta(key, value)     // сохранить метаданные
getMeta(key)            // получить метаданные
destroy()               // удалить
show()/hide()           // видимость
getPosition()           // позиция
setPosition(x, y)       // установить позицию
```

**Наследуют?** Пока никто явно, но добавляются методы сюда при необходимости

---

#### `image-manager.js` 🖼️ ИЗОБРАЖЕНИЯ
**Что?** Управление изображениями, рамками, ручками ресайза  
**Размер?** 240 строк  
**Когда менять?**
- Нужно изменить логику добавления изображений
- Нужно изменить рамку выделения
- Нужно изменить ручку ресайза
- Нужно добавить новые события (drag, scale)

**Хранит?**
```javascript
this.canvasManager          // ссылка на canvas
this.images                 // массив всех изображений
this.selectedImage          // выбранное изображение
```

**Callbacks (установлены из UIController):**
```javascript
onImageSelected(konvaImg, frame, handle)   // когда изображение выбрано
onImageMoved(konvaImg)                     // когда изображение перемещено
onImageScaled(konvaImg)                    // когда изображение масштабировано
onFrameDoubleClick(konvaImg, frame)        // двойной клик → создать точку
```

**Методы:**
```javascript
addImageFromBase64(dataUrl)             // добавить изображение
attachSelectionFrame(konvaImg)          // прикрепить рамку
sideAndOffsetToXY(img, side, offset)    // преобразовать координаты
getImages()                             // получить все
clear()                                 // очистить
```

**Где искать код:**
- Логика добавления изображения? → `addImageFromBase64()`
- Как работает рамка? → `attachSelectionFrame()`
- Ручка ресайза? → в `attachSelectionFrame()`, обработчик `dragmove`

---

#### `connection-point-manager.js` 📍 ТОЧКИ
**Что?** Управление точками соединения (pins на сторонах изображений)  
**Размер?** 260 строк  
**Когда менять?**
- Изменить логику создания точек
- Изменить цвета точек
- Изменить движение вдоль стороны
- Добавить новые события

**Хранит?**
```javascript
this.canvasManager          // ссылка на canvas
this.points                 // все точки
```

**Callbacks (установлены из UIController):**
```javascript
onPointSelected(point)      // клик по точке
onPointCreated(point)       // точка создана
onPointMoved(point)         // точка передвинута → обновить линии!
onPointDeleted(point)       // точка удалена
onPointDoubleClick(point)   // двойной клик → удалить
```

**Методы:**
```javascript
createConnectionPointOnSide(img, side, offset)  // создать точку
getNearestSideAndOffset(img, pos)               // ближайшая сторона
sideAndOffsetToXY(img, side, offset)            // преобразовать в координаты
projectAlongSide(img, side, pos)                // проекция на сторону
updatePointsForImage(img)                       // обновить позиции при движении image
deletePoint(point)                              // удалить точку
getPoints()                                     // получить все
clear()                                         // очистить
```

**Где искать код:**
- Как создается точка? → `createConnectionPointOnSide()`
- Почему точка двигается по прямой? → `projectAlongSide()`
- Как точки прикреплены к изображению? → `imageNode._cp_points` массив

---

#### `connection-manager.js` 🔗 ЛИНИИ
**Что?** Управление соединениями (линиями между точками)  
**Размер?** 480 строк  
**Когда менять?**
- Изменить алгоритм рисования линий
- Изменить ортогональность
- Добавить новые типы линий
- Изменить редактирование линий

**Хранит?**
```javascript
this.canvasManager          // ссылка на canvas
this.connections            // все линии
```

**Callbacks (установлены из UIController):**
```javascript
onConnectionCreated(connection)     // линия создана
onConnectionSelected(connection)    // линия выбрана
onConnectionDeleted(connection)     // линия удалена
```

**Методы (основные):**
```javascript
createConnection(pin1, pin2)             // создать соединение
createSmartFork(pinPos, side, target)    // умная вилка (динамическая длина)
createSimpleLine(pin1, pin2)             // простая линия: 2 вилки + 1 сегмент
updateConnectionsForPin(pin)             // пересчитать при движении пина
deleteConnection(connection)             // удалить
enforceOrthogonal(connection)            // ортогональность
addLineEditHandles(connection)           // добавить ручки для редактирования
removeLineEditHandles(connection)        // удалить ручки
getConnections()                         // получить все
clear()                                  // очистить
```

**Где искать код:**
- Как рисуется линия? → `createSimpleLine()`
- Как работают вилки? → `createSmartFork()` (умная логика длины)
- Как обновляется при движении? → `updateConnectionsForPin()`
- Как ортогональность? → `enforceOrthogonal()` (пропаганда, вилки)
- Как редактирование? → `addLineEditHandles()` (синие точки на линии)

---

#### `selection-manager.js` ✨ ВЫДЕЛЕНИЕ
**Что?** Управление выделением и подсветкой элементов  
**Размер?** 105 строк (компактный!)  
**Когда менять?**
- Изменить цвет подсветки
- Изменить алгоритм выделения
- Добавить новые типы выделения

**Хранит?**
```javascript
this.canvasManager          // ссылка на canvas
this.selected               // текущее выделение {node/connection, cleanup}
```

**Методы:**
```javascript
selectElement(node, frame, handle)      // выбрать изображение
selectConnection(connection)            // выбрать линию
clearSelection()                        // очистить выделение
getSelected()                           // получить текущее
getSelectedNode()                       // получить ноду
```

**Где искать код:**
- Как подсвечивается элемент? → `selectElement()` (синяя рамка Konva.Rect)
- Как подсвечивается линия? → `selectConnection()` (синяя Konva.Line)
- Что такое cleanup? → функция, которая удаляет подсветку

---

#### `properties-panel.js` 📝 СВОЙСТВА
**Что?** Отображение свойств выбранных элементов  
**Размер?** 78 строк (мини!)  
**Когда менять?**
- Добавить новые свойства для точек
- Добавить новые свойства для линий
- Изменить формат отображения

**Хранит?**
```javascript
this.canvasManager          // ссылка на canvas
this.container              // DOM элемент #properties-content
```

**Методы:**
```javascript
showPropertiesForPoint(point)           // показать свойства точки
showPropertiesForConnection(connection) // показать свойства линии
showDefaultMessage()                    // показать сообщение по умолчанию
clear()                                 // очистить
```

**Где искать код:**
- Что показывается для точки? → `showPropertiesForPoint()`
- Что показывается для линии? → `showPropertiesForConnection()`

---

#### `file-manager.js` 💾 ФАЙЛЫ
**Что?** Управление сохранением, загрузкой и очисткой  
**Размер?** 59 строк  
**Когда менять?**
- Реализовать сохранение в JSON
- Реализовать загрузку из JSON
- Изменить логику очистки

**Хранит?**
```javascript
this.canvasManager                  // ссылка на canvas
this.imageManager                   // ссылка на image manager
this.connectionPointManager         // ссылка на points
this.connectionManager              // ссылка на connections
```

**Методы:**
```javascript
saveScheme()            // сохранить (заглушка)
loadScheme()            // загрузить (заглушка)
clearCanvas()           // очистить canvas и все менеджеры
```

**Где искать код:**
- Логика сохранения? → `saveScheme()` (пока заглушка)
- Логика загрузки? → `loadScheme()` (пока заглушка)
- Что происходит при очистке? → `clearCanvas()` (очищает layer и все менеджеры)

---

## 📚 ДОКУМЕНТАЦИЯ

### `doc/vision.md`
**Что?** Техническое видение проекта  
**Когда читать?** Перед началом работы, чтобы понять цель  
**Содержит?**
- Описание функциональности
- Список обязательных классов
- Модель данных (Schema, Image, Connection)
- Основные сценарии использования

---

### `doc/conventions.md`
**Что?** Правила кодирования  
**Когда читать?** Перед написанием нового класса  
**Содержит?**
- Структура файла класса
- Правила именования
- Требования к импортам
- Шаблон класса
- Модульная архитектура

---

### `doc/workflow.md`
**Что?** Процесс разработки (этот файл!)  
**Когда читать?** Перед каждой итерацией  
**Содержит?**
- Этапы работы
- Требования к согласованию
- Формат коммитов
- Архитектура координации

---

### `refactoring-strategy.md`
**Что?** План рефакторинга (ВЫПОЛНЕН)  
**Статус?** ✅ Завершено  
**Содержит?**
- Описание каждого менеджера
- План разделения UIController
- Порядок реализации
- Зависимости между классами

---

### `REFACTORING_COMPLETE.md`
**Что?** Отчет о завершении рефакторинга  
**Статус?** ✅ Готов
**Содержит?**
- Список всех коммитов
- Описание каждого класса
- Размеры файлов
- Итоговые метрики

---

## 🎯 БЫСТРАЯ НАВИГАЦИЯ

### Если нужно изменить...

**...логику добавления изображений?**
→ `image-manager.js` → `addImageFromBase64()` и `attachSelectionFrame()`

**...цвета и стили подсветки?**
→ `selection-manager.js` → `selectElement()` или `selectConnection()`

**...как создаются точки соединения?**
→ `connection-point-manager.js` → `createConnectionPointOnSide()`

**...как рисуются линии?**
→ `connection-manager.js` → `createConnection()` и `createSimpleLine()`

**...как работает ортогональность?**
→ `connection-manager.js` → `enforceOrthogonal()` и `createSmartFork()`

**...как точки двигаются по стороне?**
→ `connection-point-manager.js` → `projectAlongSide()`

**...как обновляются линии при движении точки?**
→ `connection-manager.js` → `updateConnectionsForPin()`

**...как панель свойств показывает данные?**
→ `properties-panel.js` → `showPropertiesForPoint()` или `showPropertiesForConnection()`

**...UI кнопки и обработчики?**
→ `ui-controller.js` → `setupEventListeners()`

**...режим создания линий?**
→ `ui-controller.js` → `toggleLineCreationMode()` и `handlePinClickForLineCreation()`

**...связь между менеджерами?**
→ `ui-controller.js` → `setupManagerCallbacks()`

**...HTML разметку?**
→ `index.html`

**...сохранение/загрузку?**
→ `file-manager.js` → `saveScheme()` и `loadScheme()`

---

## 🔗 СВЯЗИ МЕЖДУ МЕНЕДЖЕРАМИ

```
ImageManager
    ↓ onImageSelected
UIController ←→ Callbacks ←→ SelectionManager
    ↓
ConnectionPointManager
    ↓ onPointMoved
    → ConnectionManager (обновить линии)

ConnectionManager
    ↓ onConnectionSelected
UIController ←→ SelectionManager

All Managers →
UIController → PropertiesPanel (показать свойства)
UIController → FileManager (сохранение/загрузка)
```

---

## 📊 РАЗМЕРЫ ФАЙЛОВ

| Файл | Строк | Статус |
|------|-------|--------|
| diagram-element.js | 60 | ✅ |
| image-manager.js | 240 | ✅ |
| connection-point-manager.js | 260 | ✅ |
| connection-manager.js | 480 | ✅ |
| selection-manager.js | 105 | ✅ |
| properties-panel.js | 78 | ✅ |
| file-manager.js | 59 | ✅ |
| ui-controller.js | 450 | ✅ |
| canvas-manager.js | ? | ✅ (существует) |
| **ИТОГО** | **~1900** | **✅ Готово** |

---

## ✅ КОНТРОЛЬНЫЙ СПИСОК ДЛЯ РАЗРАБОТКИ

- [ ] Все файлы на месте
- [ ] Все импорты работают
- [ ] UIController инициализирует все менеджеры
- [ ] Callbacks установлены между менеджерами
- [ ] Тестирование каждого менеджера отдельно
- [ ] Тестирование интеграции
- [ ] Проверка ортогональности линий
- [ ] Проверка обновления при движении
- [ ] Документация актуальна

---

**Последнее обновление**: 03.01.2026  
**Версия архитектуры**: 1.0 (Modular)  
**Контекст на файл (UIK)**: 450 строк ✅
