# КАРТА ФАЙЛОВ ПРОЕКТА

**Версия**: 1.2  
**Дата обновления**: 20.01.2026  
**Статус**: Актуальная с итерацией 4 (удаление сегментов)

---

## СТРУКТУРА ПРОЕКТА

```
VKR/
├── public/                           🔧 ОСНОВНОЙ КОД
│   ├── index.html                    (точка входа браузера)
│   ├── main.js                       (инициализация приложения)
│   ├── canvas-manager.js             (управление Konva.js)
│   ├── ui-controller.js              (координатор менеджеров) 
│   ├── diagram-element.js            (базовый класс элементов)
│   ├── image-manager.js              (работа с изображениями)
│   ├── connection-point-manager.js   (точки соединения)
│   ├── connection-manager.js         (линии с ортогональностью)
│   ├── connection-router.js          (поиск сегментов, конвертация)
│   ├── connection-editor.js          (редактирование разрывов на линиях)
│   ├── connection-segment-remover.js (NEW) удаление сегментов по алгоритму
│   ├── selection-manager.js          (выделение элементов)
│   ├── properties-panel.js           (панель свойств)
│   └── file-manager.js               (сохранение/загрузка)
├── docs/                             📖 ДОКУМЕНТАЦИЯ
│   ├── segment-removal-algorithm.md  (NEW) алгоритм удаления сегментов
│   └── ...
├── doc/                              СТАРАЯ ДОКУМЕНТАЦИЯ
│   ├── vision.md                     (техническое видение)
│   ├── conventions.md                (правила кодирования)
│   └── workflow.md                   (процесс разработки)
├── refactoring-strategy.md           (план рефакторинга) 
├── REFACTORING_COMPLETE.md           (отчёт о завершении) 
├── FILE_MAP.md                       (ЭТО - навигация) 
└── tasklist.md                       (список задач)
```

---

## БЫСТРАЯ НАВИГАЦИЯ

### Если нужно изменить...

**логику добавления изображений**
→ `image-manager.js` / `addImageFromBase64()`, `attachSelectionFrame()`

**цвета и стили подсветки**
→ `selection-manager.js` / `selectElement()`, `selectConnection()`

**создание точек соединения**
→ `connection-point-manager.js` / `createConnectionPointOnSide()`

**рисование линий**
→ `connection-manager.js` / `createConnection()`, `createSimpleLine()`

**ортогональность линий**
→ `connection-manager.js` / `enforceOrthogonal()`, `createSmartFork()`

**движение точек по сторонам**
→ `connection-point-manager.js` / `projectAlongSide()`

**обновление линий при движении**
→ `connection-manager.js` / `updateConnectionsForPin()`

**разрывы соединений (добавление)**
→ `connection-editor.js` / `addBreakPointToSegment()`, `editMode`

**удаление сегментов линии (NEW)**
→ `connection-segment-remover.js` / `removeSegmentAtHandle()`, `validateRemoval()`

**поиск сегментов линии**
→ `connection-router.js` / `findNearestSegment()`, `segmentsToPoints()`

**панель свойств**
→ `properties-panel.js` / `showPropertiesForPoint()`, `showPropertiesForConnection()`

**UI кнопки и события**
→ `ui-controller.js` / `setupEventListeners()`

**режим создания линий**
→ `ui-controller.js` / `toggleLineCreationMode()`, `handlePinClickForLineCreation()`

**режим редактирования соединений**
→ `ui-controller.js` / `toggleConnectionEditMode()`, `isConnectionEditMode`

**связь между менеджерами**
→ `ui-controller.js` / `setupManagerCallbacks()`

**HTML разметку**
→ `index.html`

**сохранение/загрузку**
→ `file-manager.js` / `saveScheme()`, `loadScheme()`

---

## КОМПОНЕНТЫ ИТЕРАЦИИ 4

### `connection-router.js`
**Назначение**: Утилиты для работы с сегментами линий

**Ключевые методы**:
- `findNearestSegment(segments, clickPos, tolerance)` - поиск ближайшего сегмента для разрыва
- `segmentsToPoints(segments)` - конвертация объектов сегментов в плоский массив координат
- `getProjectedPointOnSegment(segment, point)` - проекция точки на сегмент

**Когда использовать**: При поиске места для разрыва, при перерисовке линии

### `connection-editor.js`
**Назначение**: Редактирование существующих соединений (добавление разрывов)

**Ключевые методы**:
- `addBreakPointToSegment(connection, segmentIndex, clickPoint)` - добавить разрыв
- `redrawConnection(connection)` - пересчитать координаты из сегментов
- `addLineEditHandles(connection)` - создать ручки редактирования
- `removeLineEditHandles(connection)` - удалить ручки
- `updateSegmentPosition(handle, connection)` - при перетаскивании ручки

**Когда использовать**: При двойном клике на линию в режиме редактирования

### `connection-segment-remover.js` (NEW)
**Назначение**: Удаление сегментов линии по алгоритму

**Ключевые методы**:
- `removeSegmentAtHandle(connection, handleSegmentIndex)` - основная функция удаления
- `validateRemoval(points, handleSegmentIndex)` - валидация перед удалением
- `checkOrthogonality(prevPoint, nextPoint)` - проверка ортогональности
- `recalculateSegments(points)` - пересчёт сегментов из точек

**Когда использовать**: При правом клике на ручку редактирования с опцией "удалить сегмент"

**Входные данные**:
- `connection` - объект соединения Konva
- `handleSegmentIndex` - индекс сегмента (от 1 до N-2 для корректных позиций)

**Возвращает**: `boolean` (успех операции)

**Условия отказа**:
1. Ручка на крайних сегментах (затрагивает пины)
2. Минимум точек не соблюдён (Type A < 5, Type B < 6)
3. Ручка не в допустимой центральной позиции
4. Удаление приведёт к диагональному соединению

---

## ДОКУМЕНТАЦИЯ

### `docs/segment-removal-algorithm.md` (NEW) АЛГОРИТМ УДАЛЕНИЯ СЕГМЕНТОВ
**Когда читать**: Перед реализацией `connection-segment-remover.js`
**Содержит**: Полная логика валидации, классификация (Type A/B), примеры, инварианты

### `doc/vision.md` ТЕХНИЧЕСКОЕ ВИДЕНИЕ
**Когда читать**: Перед началом проекта, чтобы понять цель
**Содержит**: Описание функциональности, список классов, сценарии

### `doc/conventions.md` ПРАВИЛА КОДИРОВАНИЯ
**Когда читать**: Перед написанием нового класса
**Содержит**: Структура класса, правила именования, шаблон, модульная архитектура

### `doc/workflow.md` ПРОЦЕСС РАЗРАБОТКИ
**Когда читать**: Перед каждой итерацией
**Содержит**: Этапы работы, требования к согласованию, формат коммитов

---

## ИТЕРАЦИЯ 4: УДАЛЕНИЕ СЕГМЕНТОВ

**Проблема**:
Редактирование линий добавляло разрывы, но отсутствовала возможность их удаления.

**Решение**:
- Новый файл `connection-segment-remover.js` с полной валидацией
- Алгоритм на основе классификации Type A (нечётное N) / Type B (чётное N)
- Минимум точек (5/6) защищает пины от удаления
- Проверка ортогональности перед удалением
- Документация в `docs/segment-removal-algorithm.md`

**Архитектура**:
```
connection-editor.js
  └─ обработчик правого клика на ручку
    └─ вызывает connection-segment-remover.js
      ├─ validateRemoval() - проверки
      ├─ removeSegmentAtHandle() - логика
      └─ redrawConnection() - визуализация
```

**Ключевые файлы ветки fix/break-removal-validation**:
- `public/connection-segment-remover.js` (реализация)
- `docs/segment-removal-algorithm.md` (алгоритм)
- `FILE_MAP.md` (эта карта)

---

**Статус**: Development  
**Ветка**: `fix/break-removal-validation`  
**Пользуйся этой картой при разработке!**
