# 🗺️ КАРТА ФАЙЛОВ ПРОЕКТА

**Версия**: 1.0  
**Дата обновления**: 03.01.2026  
**Статус**: ✅ Актуальная после рефакторинга

---

## 📦 СТРУКТУРА ПРОЕКТА

```
VKR/
├── public/                           🔧 ОСНОВНОЙ КОД
│   ├── index.html                    (точка входа браузера)
│   ├── main.js                       (инициализация приложения)
│   ├── canvas-manager.js             (управление Konva.js)
│   ├── ui-controller.js              (координатор менеджеров) ⭐
│   ├── diagram-element.js            (базовый класс элементов)
│   ├── image-manager.js              (работа с изображениями)
│   ├── connection-point-manager.js   (точки соединения)
│   ├── connection-manager.js         (линии с ортогональностью)
│   ├── selection-manager.js          (выделение элементов)
│   ├── properties-panel.js           (панель свойств)
│   └── file-manager.js               (сохранение/загрузка)
├── doc/                              📚 ДОКУМЕНТАЦИЯ
│   ├── vision.md                     (техническое видение)
│   ├── conventions.md                (правила кодирования)
│   └── workflow.md                   (процесс разработки)
├── refactoring-strategy.md           (план рефакторинга) ✅
├── REFACTORING_COMPLETE.md           (отчёт о завершении) ✅
├── FILE_MAP.md                       (ЭТО - навигация) ✅
└── tasklist.md                       (список задач)
```

---

## 🎯 БЫСТРАЯ НАВИГАЦИЯ

### Если нужно изменить...

**логику добавления изображений**
→ `image-manager.js` / `addImageFromBase64()`, `attachSelectionFrame()`

**цвета и стили подсветки**
→ `selection-manager.js` / `selectElement()`, `selectConnection()`

**создание точек соединения**
→ `connection-point-manager.js` / `createConnectionPointOnSide()`

**рисование линий**
→ `connection-manager.js` / `createConnection()`, `createSimpleLine()`

**ортогональность линий**
→ `connection-manager.js` / `enforceOrthogonal()`, `createSmartFork()`

**движение точек по сторонам**
→ `connection-point-manager.js` / `projectAlongSide()`

**обновление линий при движении**
→ `connection-manager.js` / `updateConnectionsForPin()`

**панель свойств**
→ `properties-panel.js` / `showPropertiesForPoint()`, `showPropertiesForConnection()`

**UI кнопки и события**
→ `ui-controller.js` / `setupEventListeners()`

**режим создания линий**
→ `ui-controller.js` / `toggleLineCreationMode()`, `handlePinClickForLineCreation()`

**связь между менеджерами**
→ `ui-controller.js` / `setupManagerCallbacks()`

**HTML разметку**
→ `index.html`

**сохранение/загрузку**
→ `file-manager.js` / `saveScheme()`, `loadScheme()`

---

## 📋 ДЕТАЛЬНОЕ ОПИСАНИЕ ФАЙЛОВ

### `public/ui-controller.js` ⭐ ГЛАВНЫЙ КООРДИНАТОР

**Размер**: 450 строк (было 1500!) | **Статус**: ✅ Рефакторен

**Ответственность**:
- Инициализация всех менеджеров
- Установка callbacks между менеджерами
- Обработка UI-событий (клики кнопок)
- Логика режима создания линий
- Помощники для расчётов

**Методы**:
```javascript
init()                              // инициализирует все менеджеры
setupManagerCallbacks()             // связывает менеджеры вместе
setupEventListeners()               // подключает UI-события
toggleLineCreationMode()            // включит/выключить режим
handlePinClickForLineCreation()     // клик по пину в режиме
getPointerStageCoords()             // позиция курсора на canvas
getNearestSideAndOffsetFromFrame()  // ближайшая сторона рамки
```

**Когда искать код**:
- Нужно добавить новую кнопку? → `setupEventListeners()`
- Нужно изменить логику взаимодействия менеджеров? → `setupManagerCallbacks()`
- Нужно изменить режим создания линий? → `toggleLineCreationMode()`

---

### `public/image-manager.js` 🖼️ ИЗОБРАЖЕНИЯ

**Размер**: 240 строк | **Статус**: ✅ Новый

**Ответственность**:
- Добавление изображений с файла
- Создание и управление рамками выделения
- Ручки ресайза
- Обработка перемещения и масштабирования

**Callbacks** (установлены из UIController):
```javascript
onImageSelected(konvaImg, frame, handle)   // выбрано изображение
onImageMoved(konvaImg)                     // перемещено
onImageScaled(konvaImg)                    // масштабировано
onFrameDoubleClick(konvaImg, frame)        // двойной клик → создать точку
```

**Методы**:
```javascript
addImageFromBase64(dataUrl)             // добавить из файла
attachSelectionFrame(konvaImg)          // прикрепить рамку
sideAndOffsetToXY(img, side, offset)    // преобразовать координаты
getImages()                             // получить все
clear()                                 // очистить
```

**Когда искать код**:
- Как добавляется изображение? → `addImageFromBase64()`
- Как работает рамка выделения? → `attachSelectionFrame()`
- Как работает ручка ресайза? → в `attachSelectionFrame()`, обработчик `dragmove`

---

### `public/connection-point-manager.js` 📍 ТОЧКИ СОЕДИНЕНИЯ

**Размер**: 260 строк | **Статус**: ✅ Новый

**Ответственность**:
- Создание точек соединения на сторонах изображений
- Движение точек вдоль сторон (projectAlongSide)
- Привязка точек к изображениям
- Удаление точек

**Callbacks** (установлены из UIController):
```javascript
onPointSelected(point)      // клик по точке
onPointCreated(point)       // точка создана
onPointMoved(point)         // точка перемещена → обновить линии!
onPointDeleted(point)       // точка удалена
onPointDoubleClick(point)   // двойной клик → удалить
```

**Методы**:
```javascript
createConnectionPointOnSide(img, side, offset)  // создать
getNearestSideAndOffset(img, pos)               // ближайшая сторона
sideAndOffsetToXY(img, side, offset)            // координаты из параметров
projectAlongSide(img, side, pos)                // проекция на сторону
updatePointsForImage(img)                       // обновить при движении image
deletePoint(point)                              // удалить
getPoints()                                     // получить все
clear()                                         // очистить
```

**Когда искать код**:
- Как создается точка? → `createConnectionPointOnSide()`
- Почему точка не выходит за границы стороны? → `projectAlongSide()`
- Как точки привязаны к изображению? → `imageNode._cp_points` массив

---

### `public/connection-manager.js` 🔗 ЛИНИИ СОЕДИНЕНИЙ

**Размер**: 480 строк | **Статус**: ✅ Новый

**Ответственность**:
- Создание соединений между точками
- Ортогональные линии (перпендикулярные)
- Умные вилки (динамическая длина)
- Обновление при движении точек
- Редактирование (добавление сегментов)

**Callbacks** (установлены из UIController):
```javascript
onConnectionCreated(connection)     // линия создана
onConnectionSelected(connection)    // линия выбрана
onConnectionDeleted(connection)     // линия удалена
```

**Ключевые методы**:
```javascript
createConnection(pin1, pin2)             // создать соединение
createSmartFork(pinPos, side, target)    // умная вилка (динамическая длина)
createSimpleLine(pin1, pin2)             // простая линия: 2 вилки + 1 сегмент
updateConnectionsForPin(pin)             // пересчитать при движении пина
enforceOrthogonal(connection)            // ортогональность (пропагация)
addLineEditHandles(connection)           // синие точки для редактирования
removeLineEditHandles(connection)        // удалить ручки
getConnections()                         // получить все
clear()                                  // очистить
```

**Когда искать код**:
- Как рисуется линия? → `createSimpleLine()` (2 вилки + 1 сегмент)
- Как работают вилки? → `createSmartFork()` (умная логика длины)
- Как обновляется при движении? → `updateConnectionsForPin()`
- Как работает ортогональность? → `enforceOrthogonal()` (пропагация вдоль линии)
- Как редактируется? → `addLineEditHandles()` (синие точки)

---

### `public/selection-manager.js` ✨ ВЫДЕЛЕНИЕ

**Размер**: 105 строк | **Статус**: ✅ Новый

**Ответственность**:
- Выделение изображений (синяя рамка)
- Выделение линий (синяя линия)
- Подсветка при выборе
- Очистка выделения

**Методы**:
```javascript
selectElement(node, frame, handle)      // выбрать изображение
selectConnection(connection)            // выбрать линию
clearSelection()                        // очистить выделение
getSelected()                           // получить текущее
getSelectedNode()                       // получить ноду
```

**Когда искать код**:
- Как подсвечивается элемент? → `selectElement()` (синяя Konva.Rect)
- Как подсвечивается линия? → `selectConnection()` (синяя Konva.Line)
- Что такое cleanup? → функция удаления подсветки

---

### `public/properties-panel.js` 📝 СВОЙСТВА

**Размер**: 78 строк | **Статус**: ✅ Новый

**Ответственность**:
- Отображение свойств выбранного элемента
- Информация о точке
- Информация о линии
- Сообщение по умолчанию

**Методы**:
```javascript
showPropertiesForPoint(point)           // показать свойства точки
showPropertiesForConnection(connection) // показать свойства линии
showDefaultMessage()                    // сообщение по умолчанию
clear()                                 // очистить
```

**Когда искать код**:
- Что показывается для точки? → `showPropertiesForPoint()`
- Что показывается для линии? → `showPropertiesForConnection()`

---

### `public/file-manager.js` 💾 ФАЙЛЫ

**Размер**: 59 строк | **Статус**: ✅ Новый (заглушки)

**Ответственность**:
- Сохранение схемы
- Загрузка схемы
- Очистка canvas

**Методы**:
```javascript
saveScheme()            // сохранить (заглушка пока)
loadScheme()            // загрузить (заглушка пока)
clearCanvas()           // очистить canvas и все менеджеры
```

**Когда искать код**:
- Логика сохранения? → `saveScheme()` (нужно реализовать)
- Логика загрузки? → `loadScheme()` (нужно реализовать)
- Что происходит при очистке? → `clearCanvas()` (очищает layer и все менеджеры)

---

### `public/diagram-element.js` 🎨 БАЗОВЫЙ КЛАСС

**Размер**: 60 строк | **Статус**: ✅ Новый

**Ответственность**:
- Общие методы для всех элементов
- Работа с метаданными
- Управление видимостью

**Методы**:
```javascript
getShape()              // получить Konva объект
setMeta(key, value)     // сохранить метаданные
getMeta(key)            // получить метаданные
destroy()               // удалить
show()/hide()           // видимость
getPosition()           // позиция
setPosition(x, y)       // установить позицию
```

**Когда искать код**:
- Общие методы для элементов? → здесь

---

### `public/canvas-manager.js` 🎭 УПРАВЛЕНИЕ CANVAS

**Размер**: ? | **Статус**: ✅ Существует (не менять без нужды)

**Ответственность**:
- Инициализация Konva stage
- Управление layer
- Добавление сетки
- Pan/zoom логика

**Методы**:
```javascript
getStage()          // получить Konva stage
getLayer()          // получить Konva layer
addGrid()           // нарисовать сетку
```

---

## 📚 ДОКУМЕНТАЦИЯ

### `doc/vision.md` 👀 ТЕХНИЧЕСКОЕ ВИДЕНИЕ
**Когда читать**: Перед началом проекта, чтобы понять цель
**Содержит**: Описание функциональности, список классов, сценарии

### `doc/conventions.md` 📏 ПРАВИЛА КОДИРОВАНИЯ
**Когда читать**: Перед написанием нового класса
**Содержит**: Структура класса, правила именования, шаблон, модульная архитектура

### `doc/workflow.md` 🔄 ПРОЦЕСС РАЗРАБОТКИ
**Когда читать**: Перед каждой итерацией
**Содержит**: Этапы работы, требования к согласованию, формат коммитов

### `refactoring-strategy.md` 🔧 ПЛАН РЕФАКТОРИНГА
**Статус**: ✅ ВЫПОЛНЕН
**Содержит**: Описание каждого менеджера, порядок реализации, зависимости

### `REFACTORING_COMPLETE.md` ✅ ОТЧЁТ О ЗАВЕРШЕНИИ
**Статус**: ✅ ГОТОВ
**Содержит**: Все коммиты, описание каждого класса, размеры файлов

---

## 📊 РАЗМЕРЫ ФАЙЛОВ

| Файл | Строк | Статус |
|------|-------|--------|
| diagram-element.js | 60 | ✅ |
| image-manager.js | 240 | ✅ |
| connection-point-manager.js | 260 | ✅ |
| connection-manager.js | 480 | ✅ |
| selection-manager.js | 105 | ✅ |
| properties-panel.js | 78 | ✅ |
| file-manager.js | 59 | ✅ |
| ui-controller.js | 450 | ✅ |
| canvas-manager.js | ? | ✅ (существует) |
| **ИТОГО** | **~1900** | **✅** |

---

## 🎓 ИНСТРУКЦИЯ ДЛЯ НОВИЧКА

1. **Прочитайте эту карту полностью**
2. **Изучите** `doc/conventions.md` для правил кодирования
3. **Поймите** `doc/workflow.md` для процесса разработки
4. **Когда нужна новая функция**: используйте "Быструю навигацию" выше
5. **Если путаетесь в коде**: найдите метод в этом файле, там указан точный путь

---

## ✅ КОНТРОЛЬНЫЙ СПИСОК

- [ ] Прочитал эту карту полностью
- [ ] Может найти нужный файл за 30 секунд
- [ ] Понимает архитектуру (9 менеджеров)
- [ ] Знаю как работают callbacks
- [ ] Готов к разработке

---

**Статус**: ✅ Production-Ready  
**Контекст на файл (UIController)**: 450 строк (было 1500!)  
**Пользуйся этой картой при разработке!** 🚀
