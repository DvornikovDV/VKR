# План реализации виджетов по итерациям

**Версия**: 2.0  
**Дата обновления**: 28.01.2026  
**Статус**: Актуальный план на основе widgets-dev-guide.md раздел 7  
**Язык**: Русский  

---

## Обзор плана

Система виджетов разделена на **7 итераций** (15 рабочих дней).

**Виджеты (9 типов):**
- **Display (4)**: number-display, text-display, led, gauge
- **Input (2)**: number-input, text-input  
- **Control (3)**: toggle, button, slider

**Фокус:** конструктор (редактор), БЕЗ реалтайм данных

---

## Итерация 1-2: Основы Display + позиция/размер/выделение (5 дней)

**Цель объединённых итераций:** Первые Display виджеты (4 типа) + базовое редактирование свойств

### Виджеты в этих итерациях
- ✅ number-display (числовой дисплей)
- ✅ text-display (текстовый дисплей)
- ✅ led (светодиод/индикатор)
- ✅ gauge (манометр/циферблат)

### Статус
- **Итерация 1** (3 дня): Display основы — **✅ 100% готово**
- **Итерация 2** (2 дня): Позиция, размер, выделение — **✅ 100% готово**

### Задачи Итерации 1: Основы Display

#### Создание и удаление виджетов
- [x] Создать класс `WidgetManager` (~500 строк)
  - [x] Инициализация с layer, imageManager, canvasManager
  - [x] Метод `create(config)` — создание нового виджета
  - [x] Метод `delete(widgetId)` — удаление виджета
  - [x] Метод `getWidget(widgetId)` — получить виджет по ID
  - [x] Метод `getWidgetsByImageId(imageId)` — получить все виджеты на изображении

#### Визуализация на Konva
- [x] Создать `widget-types.js` — определение всех 9 типов
  - [x] Класс `Widget` (базовый)
  - [x] Класс `NumberDisplay` — число с единицей
  - [x] Класс `TextDisplay` — текстовый вывод
  - [x] Класс `LED` — индикатор ON/OFF (круг)
  - [x] Класс `Gauge` — стрелочный индикатор

#### Контекстное меню
- [x] Создать `context-menu.js` — переиспользуемое меню
  - [x] Метод `show(items, x, y)` — отобразить меню в позиции
  - [x] Метод `hide()` — скрыть меню
  - [x] Обработка клика на пункт
- [x] Обновить `image-manager.js`
  - [x] Правый клик по изображению → меню
  - [x] Пункт "Добавить виджет" с подменю (4 Display типа)
  - [x] Вызов `widgetManager.create()` при выборе

#### Панель свойств (базовая версия)
- [x] Обновить `properties-panel.js`
  - [x] Метод `showPropertiesForWidget(widget)` — показать свойства
  - [x] Показать: ID, тип, категория (read-only)
  - [x] Показать: позиция X, Y (editable)
  - [x] Показать: ширина, высота (editable) — для Display
  - [x] Показать: размер шрифта (editable) — для Number/Text Display
  - [x] Показать: цвет текста (color picker) — для Number/Text Display
  - [x] Показать: цвет фона (color picker) — для Number/Text Display
  - [x] Показать: радиус (editable) — для LED
  - [x] Показать: цвет ON/OFF (color picker) — для LED
  - [x] Показать выпадающий список устройств (пока пусто)

#### Выделение виджетов
- [x] Обновить `selection-manager.js`
  - [x] Метод `selectWidget(widget)` — выбрать виджет
  - [x] Визуальное выделение: синяя рамка вокруг виджета
  - [x] Обработка клика на widget.konvaGroup
  - [x] Метод `clearSelection()` — снять выделение
  - [x] Запомнить выбранный виджет в `this.selected`

#### Интеграция UI
- [x] Обновить `ui-controller.js`
  - [x] Инициализация WidgetManager в конструкторе
  - [x] Привязка WidgetManager к imageManager через setContextMenu()
  - [x] В `setupManagerCallbacks()`:
    - [x] `widgetManager.onWidgetSelected` → SelectionManager.selectWidget()
    - [x] `widgetManager.onWidgetSelected` → PropertiesPanel.showPropertiesForWidget()
  - [x] В `deleteSelected()`:
    - [x] Добавить обработку удаления виджета: `widgetManager.delete(widget.id)`
  - [x] Обновить при ресайзе изображения: вызывать `widgetManager.onImageResize()`

### Задачи Итерации 2: Позиция, размер, выделение

#### Позиция и размер с проверками
- [x] Реализовать в `widget-manager.js`
  - [x] Метод `updatePosition(widgetId, newX, newY)` — обновить позицию
    - [x] Применить граничные проверки (виджет не выходит за границы изображения)
    - [x] Обновить относительные координаты (relativeX, relativeY)
    - [x] Обновить Konva группу
  - [x] Метод `_applyBoundaryConstraints(widget, x, y)` — проверка границ
  - [x] Метод `updateSize(widgetId, newWidth, newHeight)` — обновить размер
    - [x] Применить минимальные размеры (10x10)
    - [x] Проверить, чтобы виджет остался в границах
    - [x] Перерисовать виджет

#### Драг-энд-дроп
- [x] Реализовать в `widget-manager.js`
  - [x] Метод `attachDragHandlers(widget)` — привязать обработчики
  - [x] На dragstart: запомнить стартовую позицию
  - [x] На dragmove: немедленно применить граничные проверки
  - [x] На dragend: логировать движение
  - [x] konvaGroup.draggable(true)

#### Полная панель свойств
- [x] Обновить `properties-panel.js`
  - [x] Расширить раздел "Позиция и размер"
    - [x] X, Y как editable inputs
    - [x] Width, Height как editable inputs
  - [x] Раздел "Оформление"
    - [x] Размер шрифта (editable) — для Number/Text Display
    - [x] Цвет текста (color picker) — для Number/Text Display
    - [x] Цвет фона (color picker) — для Number/Text Display
    - [x] Для LED: радиус, цвет ON, цвет OFF (color picker)
  - [x] Раздел "Привязка устройства" (выпадающий список)

#### Синхронизация с SelectionManager
- [x] Интеграция в `ui-controller.js`
  - [x] При клике на widget → SelectionManager.selectWidget(widget)
  - [x] При выборе → PropertiesPanel.showPropertiesForWidget(widget)
  - [x] При клике на canvas (не на виджет) → снять выделение
  - [x] Delete-кнопка удаляет выбранный виджет

#### Масштабирование при ресайзе изображения
- [x] Метод `onImageResize()` в widget-manager.js
  - [x] Пересчитать позицию по относительным координатам
  - [x] Масштабировать размер виджета пропорционально
  - [x] ✅ Работает корректно

#### Тестирование
- [x] Создать по одному виджету каждого типа
- [x] Выбрать виджет → панель свойств показывает данные
- [x] Отредактировать позицию и размер → виджет изменился
- [x] ✅ После изменения свойства виджет остается выделяемым и перемещаемым
- [x] Перетянуть за границу → остановился на границе
- [x] Удалить виджет через кнопку → пропал
- [x] ✅ Ресайз изображения → виджеты корректно масштабируются с сохранением относительных позиций
- [x] ✅ Drag изображения (NOT implemented - не требуется) — виджеты НЕ привязаны к движению

### Результаты (если завершены обе итерации 1-2)

- ✅ 4 Display типа видны на экране
- ✅ Контекстное меню работает (правый клик)
- ✅ Виджеты создаются в центре изображения
- ✅ Виджеты выбираются (выделение)
- ✅ Панель свойств показывает и позволяет менять параметры
- ✅ Виджеты перемещаются внутри изображения (drag)
- ✅ Виджеты удаляются
- ✅ Все работает без выхода за границы
- ✅ Виджеты остаются реактивными после изменения свойств
- ✅ LED имеет правильные свойства (радиус, цвет ON/OFF)
- ✅ Масштабирование при ресайзе изображения работает корректно

### Реализованные файлы

- [x] `public/widget-manager.js` — создан
- [x] `public/widget-types.js` — создан
- [x] `public/context-menu.js` — создан
- [x] `public/image-manager.js` — обновлён (меню)
- [x] `public/properties-panel.js` — ОБНОВЛЕН (удалена кнопка delete)
- [x] `public/selection-manager.js` — обновлен
- [x] `public/ui-controller.js` — обновлён (инициализация)

---

## Итерация 3: Input виджеты (2 дня)

**Цель:** Типы для ввода данных

### Виджеты в этой итерации
- ⬜ number-input (числовое поле)
- ⬜ text-input (текстовое поле)

### Задачи

#### Реализация Input типов
- [ ] Добавить в `widget-types.js`
  - [ ] Класс `NumberInput` — ввод числа с валидацией min/max
  - [ ] Класс `TextInput` — ввод текста с валидацией pattern
  - [ ] Визуализация как текстовое поле на схеме

#### Валидация
- [ ] На уровне класса InputWidget
  - [ ] Проверка min/max для number-input
  - [ ] Проверка maxLength и pattern для text-input
  - [ ] Отклонение некорректных значений

#### Панель свойств
- [ ] Расширить `properties-panel.js`
  - [ ] Раздел "Параметры ввода"
    - [ ] Number-input: Min, Max, Step (editable)
    - [ ] Text-input: MaxLength, Pattern (editable)
    - [ ] Placeholder текст

#### Контекстное меню
- [ ] Обновить `image-manager.js`
  - [ ] Добавить 2 Input типа в меню
  - [ ] Всего теперь 6 типов в меню

#### Тестирование
- [ ] Создать number-input и text-input
- [ ] Ввести различные значения (валидные и невалидные)
- [ ] Проверить граничные значения
- [ ] Панель свойств показывает параметры

### Результаты

- ✅ Input типы видны на экране
- ✅ Валидация работает
- ✅ Контекстное меню имеет 6 типов

---

## Итерация 4: Control виджеты (2 дня)

**Цель:** Интерактивные управляющие компоненты

### Виджеты в этой итерации
- ⬜ toggle (переключатель ON/OFF)
- ⬜ button (кнопка — отправить команду)
- ⬜ slider (ползунок — выбрать число)

### Задачи

#### Реализация Control типов
- [ ] Добавить в `widget-types.js`
  - [ ] Класс `Toggle` — переключатель с состоянием
  - [ ] Класс `Button` — кнопка с меткой
  - [ ] Класс `Slider` — ползунок с диапазоном
  - [ ] Визуализация каждого типа

#### Интерактивность
- [ ] На уровне WidgetManager
  - [ ] Обработчики click, drag для каждого типа
  - [ ] Фидбек: изменение визуального состояния при действии
  - [ ] Toggle: меняет состояние и цвет
  - [ ] Button: реагирует на клик (визуальная реакция)
  - [ ] Slider: обновляет значение при drag

#### Панель свойств
- [ ] Расширить `properties-panel.js`
  - [ ] Раздел "Параметры управления"
    - [ ] Toggle: метка ON, метка OFF, цвет ON, цвет OFF
    - [ ] Button: текст кнопки, цвет фона
    - [ ] Slider: Min, Max, Step, текущее значение

#### Контекстное меню
- [ ] Обновить `image-manager.js`
  - [ ] Добавить 3 Control типа в меню
  - [ ] Всего теперь 9 типов в меню (финальный набор)

#### Тестирование
- [ ] Создать все 3 типа
- [ ] Toggle: кликнуть → меняет состояние
- [ ] Button: кликнуть → видна визуальная реакция
- [ ] Slider: перетянуть → обновляется значение
- [ ] Панель свойств показывает параметры
- [ ] Все 9 типов доступны в меню

### Результаты

- ✅ Все 9 типов доступны
- ✅ Control виджеты интерактивны
- ✅ Контекстное меню полное

---

## Итерация 5: Синхронизация с изображениями (2 дня)

**Цель:** Виджеты движутся и масштабируются вместе с изображениями

### Задачи

#### Движение с изображением
- [x] Реализовать в `widget-manager.js`
  - [x] Метод `onImageMove(imageId, deltaX, deltaY)` — при движении изображения
    - [x] Обновить позицию всех виджетов на этом изображении
    - [x] Сохранить относительные координаты
    - [x] Перерисовать

#### Масштабирование с изображением
- [x] Реализовать в `widget-manager.js`
  - [x] Метод `onImageResize(imageId, newWidth, newHeight)` — при ресайзе
    - [x] Пересчитать позицию каждого виджета по относительным координатам
    - [x] Масштабировать размер виджета
    - [x] Применить граничные проверки
    - [x] Перерисовать

#### Удаление вместе с изображением
- [x] Реализовать в `widget-manager.js`
  - [x] Метод `onImageDelete(imageId)` — при удалении изображения
    - [x] Удалить все виджеты на этом изображении

#### Интеграция с ImageManager
- [x] Обновить `image-manager.js`
  - [x] При вызове updateImage() → вызвать widgetManager.onImageMove()
  - [x] При вызове resizeImage() → вызвать widgetManager.onImageResize()
  - [x] При вызове deleteImage() → вызвать widgetManager.onImageDelete()

#### Тестирование
- [x] Переместить изображение → виджеты движутся с ним
- [x] Ресайзить изображение → виджеты масштабируются
- [x] Проверить относительные координаты (если изображение в новой позиции → виджеты на месте относительно изображения)
- [x] Удалить изображение → все его виджеты исчезают

### Результаты

- ✅ Виджеты синхронизированы с изображениями
- ✅ При движении/ресайзе/удалении изображения виджеты обновляются

---

## Итерация 6: Привязка к устройствам (2 дня)

**Цель:** Связь виджетов с ID устройств и метаданными

### Задачи

#### Реестр устройств
- [ ] Создать mock `backend/config/devices-registry.json`
  ```json
  {
    "devices": [
      {
        "id": "sensor_temp_01",
        "name": "Датчик температуры №1",
        "type": "sensor",
        "unit": "°C",
        "min": -10,
        "max": 50,
        "description": "Датчик в теплице №1",
        "dataType": "float",
        "mqttTopic": "greenhouse/temp/01"
      },
      ...
    ]
  }
  ```
- [ ] Загрузить в UIController на инициализацию

#### Привязка в WidgetManager
- [ ] Реализовать в `widget-manager.js`
  - [ ] Метод `updateBinding(widgetId, deviceId)` — привязать виджет к устройству
  - [ ] Сохранить deviceId в widget.bindingId
  - [ ] Получить метаданные устройства

#### Панель свойств
- [ ] Расширить `properties-panel.js`
  - [ ] Раздел "Привязка устройства"
    - [ ] Выпадающий список всех устройств
    - [ ] Опция "-- не привязано --"
    - [ ] При выборе → показать метаданные:
      - [ ] Единица измерения (unit)
      - [ ] Диапазон (min, max)
      - [ ] Описание
      - [ ] Тип устройства
      - [ ] MQTT топик (read-only)
      - [ ] Тип данных (read-only)

#### **ВАЖНО: БЕЗ РЕАЛТАЙМА**
- [ ] НЕ показывать текущее значение от датчика
- [ ] НЕ показывать live обновления
- [ ] НЕ подключаться к реальным данным
- [ ] Только статические метаданные из registry

#### Тестирование
- [ ] Выпадающий список показывает все устройства из registry
- [ ] Выбрать устройство → метаданные отображаются
- [ ] Разные виджеты привязаны к разным устройствам
- [ ] Отвязка (выбрать "не привязано") работает
- [ ] НЕТ live данных в панели

### Результаты

- ✅ Выпадающий список с устройствами
- ✅ Метаданные отображаются
- ✅ Привязка сохраняется в памяти

---

## Итерация 7: Интеграция и полировка (2 дня)

**Цель:** Все компоненты работают вместе, готово к save/load

### Задачи

#### Полное тестирование
- [ ] Полный workflow: создание → редактирование → удаление
  - [ ] Создать виджет через контекстное меню
  - [ ] Выбрать виджет → показана панель свойств
  - [ ] Отредактировать все параметры
  - [ ] Переместить внутри изображения (drag)
  - [ ] Переместить изображение → виджеты движутся
  - [ ] Ресайзить изображение → виджеты масштабируются
  - [ ] Удалить виджет → пропал

#### Edge cases
- [ ] Удалить изображение с виджетами → все удалились
- [ ] Ввести некорректные значения → валидация отклонила
- [ ] Переместить виджет за границу → граничная проверка остановила
- [ ] Ресайзить в ноль → минимальный размер соблюдён

#### Обработка ошибок
- [ ] Все методы имеют проверки на null/undefined
- [ ] Логирование при ошибках
- [ ] Graceful fallback при отсутствии элементов

#### Визуальная полировка
- [ ] Иконки в контекстном меню (🔘 📊 💡 и т.д.)
- [ ] Консистентные цвета (UI style guide)
- [ ] Иконки кнопок в toolbar
- [ ] Шрифты и размеры (типография)
- [ ] Spacing и padding
- [ ] Состояния элементов (hover, active, disabled)

#### Проверка памяти
- [ ] Нет утечек при удалении виджетов
- [ ] Очистка слушателей событий при удалении
- [ ] Проверить DevTools Memory profiler

#### Документирование
- [ ] JSDoc комментарии ко всем методам
- [ ] README для модуля виджетов
- [ ] Описание архитектуры
- [ ] Примеры использования

#### Финальная проверка
- [ ] Нет консольных ошибок/предупреждений
- [ ] Все тесты из Testing Checklist пройдены
- [ ] Performance OK (нет лагов)
- [ ] На разных разрешениях экрана

### Результаты

- ✅ Система виджетов полностью функциональна
- ✅ Готово к разработке save/load
- ✅ Готово к backend интеграции
- ✅ Код документирован и полирован

---

## Таблица общего прогресса

| Итерация | Название | Дни | Виджеты | Статус |
|----------|----------|-----|---------|--------|
| 1-2 | Display + позиция/размер/выделение | 5 | 4 Display | ✅ 100% |
| 3 | Input виджеты | 2 | number-input, text-input | ⬜ 0% |
| 4 | Control виджеты | 2 | toggle, button, slider | ⬜ 0% |
| 5 | Синхронизация с изображениями | 2 | все 9 типов | ⬜ 0% |
| 6 | Привязка к устройствам | 2 | все 9 типов | ⬜ 0% |
| 7 | Интеграция и полировка | 2 | все 9 типов | ⬜ 0% |
| **ИТОГО** | **Виджеты готовы** | **15** | **9 типов** | ✅ 33% |

---

## Файлы, требующие обновления в Итерациях 1-2

### Уже готовы ✅
- `public/widget-manager.js` — создан
- `public/widget-types.js` — создан
- `public/context-menu.js` — создан
- `public/ui-controller.js` — обновлён (инициализация WidgetManager)
- `public/image-manager.js` — обновлён (привязка контекстного меню)
- `public/properties-panel.js` — обновлен (удалена кнопка delete, LED свойства)
- `public/selection-manager.js` — обновлен

---

## Ключевые принципы

### ✅ Запомнить

1. **БЕЗ реалтайма в конструкторе** — только структура и метаданные
2. **Контекстное меню** — правый клик по изображению (без обязательного выделения)
3. **Автовыбор после создания** — новый виджет сразу выбирается
4. **Относительные координаты** — при ресайзе изображения виджеты масштабируются
5. **Слой виджетов выше изображений** — всегда видны
6. **Граничные проверки** — виджеты не выходят за границы
7. **Синхронизация с изображениями** — движутся и масштабируются вместе ПРИ РЕСАЙЗЕ
8. **9 типов виджетов** — 4 Display (R) + 2 Input (W) + 3 Control (RW)
9. **Разные классы** — каждый тип в своём классе
10. **Валидация по типам** — Display валидирует отображение, Input валидирует ввод
11. **Удаление через общую кнопку** — в панели свойств кнопки удаления нет

### ❌ Избежать

- ❌ Сохранение в JSON (отдельная фаза)
- ❌ Backend интеграция (отдельная фаза)
- ❌ Реалтайм данные от датчиков
- ❌ Объединение Display + Input в один компонент
- ❌ Виджеты выходят за границы
- ❌ Дублирование кода вместо наследования
- ❌ Реалтайм значения в панели админа
- ❌ Кнопка удаления в панели свойств

---

## Что дальше после завершения

После 7 итераций система виджетов готова к:

1. **Фаза: Save/Load** — сохранение конфигурации в JSON
2. **Фаза: Backend** — MQTT, WebSocket, API для реалтайм данных
3. **Фаза: Dashboard** — оператор видит live значения и управляет

---

**Документ обновляется по мере разработки.**
