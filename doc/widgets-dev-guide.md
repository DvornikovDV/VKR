# Разработка виджетов (интерактивных элементов)

**Версия**: 1.0  
**Дата**: 22.01.2026  
**Статус**: Development Guide  
**Область**: Только виджеты (без save/load и backend)

---

## Обзор

Разработка системы виджетов для редактора мнемосхем. Виджеты - интерактивные элементы (кнопки, индикаторы, дисплеи), которые размещаются на изображениях.

**Что входит в эту фазу:**
- Создание, редактирование, удаление виджетов
- Размещение на изображениях
- Привязка к устройствам (в памяти, без save)
- Визуализация в редакторе

**Что НЕ входит:**
- Сохранение/загрузка JSON (отдельная фаза)
- Backend, MQTT, WebSocket (отдельная фаза)

---

## 1. ДОБАВЛЕНИЕ ВИДЖЕТОВ

### Способ: Контекстное меню (правый клик по изображению)

При правом клике по изображению появляется меню с опциями.

### Вопрос: Выделение изображения

**Ответ: БЕЗ обязательного выделения**

Причины:
- ПКМ уже указывает на конкретное изображение
- Выделение может помешать видеть структуру
- Меню достаточно контекстного сигнала

**Поведение:**
```
1. ПКМ на изображение
2. Меню появляется
3. Выбрать "Добавить виджет" → тип
4. Виджет создан, АВТОМАТИЧЕСКИ выбран
5. Панель свойств открыта справа
```

**Виджет автоматически выбирается**, но изображение может остаться не выделенным.

### Структура меню

```
ПКМ на изображение
└── Добавить виджет ▶
    ├── 🔘 Переключатель (toggle)
    ├── 💡 Индикатор (led)
    ├── 📊 Числовой дисплей (number-display)
    ├── 📝 Текстовый дисплей (text-display)
    ├── 🎚️ Ползунок (slider)
    └── 📈 Манометр (gauge)
├─── Свойства
├─── Удалить
└─── Дублировать
```

### Реализация

```javascript
// image-manager.js
setupImageContextMenu(imageNode, imageId) {
  imageNode.on('contextmenu', (e) => {
    e.evt.preventDefault();
    
    const menu = [
      {
        label: 'Добавить виджет',
        submenu: [
          { label: '🔘 Переключатель', type: 'toggle' },
          { label: '💡 Индикатор', type: 'led' },
          { label: '📊 Числовой дисплей', type: 'number-display' },
          { label: '📝 Текстовый дисплей', type: 'text-display' },
          { label: '🎚️ Ползунок', type: 'slider' },
          { label: '📈 Манометр', type: 'gauge' }
        ],
        onSelect: (type) => this.createWidgetOnImage(imageId, type)
      },
      { separator: true },
      {
        label: 'Свойства',
        onClick: () => propertiesPanel.showImageProperties(imageId)
      },
      {
        label: 'Удалить',
        onClick: () => this.deleteImage(imageId)
      },
      {
        label: 'Дублировать',
        onClick: () => this.duplicateImage(imageId)
      }
    ];
    
    contextMenu.show(menu, e.evt.clientX, e.evt.clientY);
  });
}

creatWidgetOnImage(imageId, widgetType) {
  const image = this.getImage(imageId);
  
  // Создать в центре изображения
  const widget = widgetManager.create({
    type: widgetType,
    imageId: imageId,
    x: image.x + image.width / 2,
    y: image.y + image.height / 2,
    width: 80,
    height: 30
  });
  
  // Виджет автоматически выбран
  selectionManager.select(widget);
  
  // Показать свойства
  propertiesPanel.show(widget);
}
```

---

## 2. ТИПЫ ВИДЖЕТОВ

### Доступные типы

| Тип | ID | Назначение | Read/Write |
|-----|----|-----------|-----------|
| Переключатель | toggle | Управление включение/выключение | RW |
| Кнопка | button | Одноразовое действие | W |
| Индикатор | led | Показ статуса on/off | R |
| Числовой дисплей | number-display | Отображение числа | R |
| Текстовый дисплей | text-display | Отображение текста | R |
| Ползунок | slider | Управление числовым значением | RW |
| Манометр | gauge | Отображение стрелочного значения | R |

### Визуальное представление в коде

```javascript
// widget-types.js
const WIDGET_DEFAULTS = {
  toggle: {
    width: 50,
    height: 30,
    color: '#2196F3',
    onLabel: 'ON',
    offLabel: 'OFF'
  },
  led: {
    width: 40,
    height: 40,
    radius: 20,
    colorOff: '#ccc',
    colorOn: '#4caf50'
  },
  'number-display': {
    width: 100,
    height: 30,
    fontSize: 16,
    decimals: 1,
    unit: ''
  },
  'text-display': {
    width: 120,
    height: 25,
    fontSize: 14,
    text: 'Label'
  },
  slider: {
    width: 150,
    height: 40,
    min: 0,
    max: 100,
    step: 1
  },
  gauge: {
    width: 80,
    height: 80,
    min: 0,
    max: 100
  }
};
```

---

## 3. ПАНЕЛЬ СВОЙСТВ ВИДЖЕТА

### Структура

```
┌─────────────────────────────────────┐
│     СВОЙСТВА ВИДЖЕТА                │
├─────────────────────────────────────┤
│ ▼ Общие                             │
│   ID: widget_temp_1                 │
│   Тип: Числовой дисплей            │
│   На изображении: Насос (img_pump_1)│
│                                     │
│ ▼ Позиция и размер                  │
│   X: [150]  Y: [50]                │
│   Ширина: [80]  Высота: [30]       │
│                                     │
│ ▼ Оформление                        │
│   Размер шрифта: [14] px           │
│   Цвет текста: [#000000] [выбрать] │
│   Выравнивание: [центр ▼]          │
│                                     │
│ ▼ Привязка устройства               │
│   ┌──────────────────────────────┐ │
│   │ -- не привязано --        ▼ │ │
│   └──────────────────────────────┘ │
│                                     │
│   Выпадающий список:                │
│   ├─ (не привязано)                │
│   ├─ greenhouse_01.sensors.temp    │
│   ├─ greenhouse_01.sensors.humidity│
│   └─ greenhouse_01.devices.pump    │
│                                     │
│   Метаданные устройства (если    │
│   привязано):                      │
│   ├─ Единица: °C                  │
│   ├─ Диапазон: -10 до 50°C        │
│   ├─ Описание: Датчик в теплице   │
│   └─ Тип: sensor                  │
│                                     │
│ ▼ Дополнительно                     │
│   Формат: [0.0 ▼]                 │
│                                     │
└─────────────────────────────────────┘
```

### Важно: БЕЗ реалтайма в редакторе

**НЕ показываем:**
- ❌ Последнее значение от датчика
- ❌ Текущее состояние устройства
- ❌ Live обновления

**Показываем только:**
- ✅ Метаданные устройства (единица, диапазон, описание)
- ✅ Структура и конфигурация

**Причина:** Реалтайм данные - это задача Dashboard'а для оператора, не админа.

### Реализация панели

```javascript
// properties-panel.js
showWidgetProperties(widget) {
  const html = `
    <div class="properties-panel">
      <h3>Свойства виджета</h3>
      
      <section class="prop-section">
        <h4>Общие</h4>
        <div class="prop-item">
          <label>ID:</label>
          <input type="text" value="${widget.id}" disabled>
        </div>
        <div class="prop-item">
          <label>Тип:</label>
          <input type="text" value="${widget.type}" disabled>
        </div>
        <div class="prop-item">
          <label>На изображении:</label>
          <input type="text" value="${imageManager.getImage(widget.imageId).name}" disabled>
        </div>
      </section>
      
      <section class="prop-section">
        <h4>Позиция и размер</h4>
        <div class="prop-row">
          <div class="prop-item">
            <label>X:</label>
            <input type="number" value="${Math.round(widget.x)}" onchange="updateWidgetPosition()">
          </div>
          <div class="prop-item">
            <label>Y:</label>
            <input type="number" value="${Math.round(widget.y)}" onchange="updateWidgetPosition()">
          </div>
        </div>
        <div class="prop-row">
          <div class="prop-item">
            <label>Ширина:</label>
            <input type="number" value="${widget.width}" onchange="updateWidgetSize()">
          </div>
          <div class="prop-item">
            <label>Высота:</label>
            <input type="number" value="${widget.height}" onchange="updateWidgetSize()">
          </div>
        </div>
      </section>
      
      <section class="prop-section">
        <h4>Оформление</h4>
        <div class="prop-item">
          <label>Размер шрифта:</label>
          <input type="number" value="${widget.fontSize || 14}" onchange="updateStyle()">
        </div>
        <div class="prop-item">
          <label>Цвет текста:</label>
          <input type="color" value="${widget.color || '#000000'}" onchange="updateStyle()">
        </div>
      </section>
      
      <section class="prop-section">
        <h4>Привязка устройства</h4>
        <select onchange="onDeviceSelected(event)">
          <option value="">-- не привязано --</option>
          ${this.getAvailableDevices().map(device => `
            <option value="${device.id}" ${widget.bindingId === device.id ? 'selected' : ''}>
              ${device.name}
            </option>
          `).join('')}
        </select>
        
        ${widget.bindingId ? `
          <div class="metadata-display">
            <h5>Метаданные:</h5>
            <p>Единица: <strong>${this.getDeviceMetadata(widget.bindingId).unit}</strong></p>
            <p>Диапазон: <strong>${this.getDeviceMetadata(widget.bindingId).min} - ${this.getDeviceMetadata(widget.bindingId).max}</strong></p>
            <p>Описание: <strong>${this.getDeviceMetadata(widget.bindingId).description}</strong></p>
            <p>Тип: <strong>${this.getDeviceMetadata(widget.bindingId).type}</strong></p>
          </div>
        ` : ''}
      </section>
    </div>
  `;
  
  document.getElementById('properties-panel').innerHTML = html;
}
```

---

## 4. ОГРАНИЧЕНИЯ И ПРАВИЛА

### Драг виджетов

✅ **Разрешено:** Перемещение только внутри изображения  
✅ **Разрешено:** Автоматический drag в пределах границ  
❌ **Запрещено:** Перемещение вне изображения  
❌ **Запрещено:** Перетягивание на другое изображение  

### Размещение при перемещении изображения

✅ Виджеты движутся вместе с изображением  
✅ Относительная позиция сохраняется  
✅ При ресайзе изображения виджеты масштабируются

### Слои (Z-порядок)

```
слой 4: Handles (крестики для редактирования)
слой 3: Widgets (выше изображений)
слой 2: Connections (линии)
слой 1: Images (внизу)
```

**Важно:** Виджеты ВСЕГДА видны над изображениями.

---

## 5. АРХИТЕКТУРА КОДА

### Основные классы

```
public/js/
├── widget-manager.js          (ядро - управление виджетами)
├── widget-types.js            (определение типов)
├── context-menu.js            (контекстное меню)
├── properties-panel.js        (панель свойств)
├── image-manager.js           (обновить - привязать меню)
└── selection-manager.js       (обновить - поддержка виджетов)
```

### WidgetManager

```javascript
class WidgetManager {
  constructor(layer, imageManager) {
    this.layer = layer;
    this.imageManager = imageManager;
    this.widgets = []; // массив всех виджетов
  }
  
  create(config) {
    // Создать новый виджет
    // Добавить на слой
    // Привязать обработчики dragging
    // Вернуть объект widget
  }
  
  delete(widgetId) {
    // Удалить виджет
  }
  
  getWidget(widgetId) {
    // Получить объект виджета
  }
  
  getWidgetsByImageId(imageId) {
    // Получить все виджеты на изображении
  }
  
  updatePosition(widgetId, x, y) {
    // Обновить позицию (с граничными проверками)
  }
  
  updateSize(widgetId, width, height) {
    // Обновить размер
  }
  
  updateBinding(widgetId, deviceId) {
    // Обновить привязку к устройству
  }
  
  onImageMove(imageId, deltaX, deltaY) {
    // При перемещении изображения, двинуть его виджеты
  }
  
  onImageResize(imageId, newWidth, newHeight) {
    // При ресайзе изображения, масштабировать виджеты
  }
}
```

---

## 6. ПЛАН РЕАЛИЗАЦИИ ПО ИТЕРАЦИЯМ

### Итерация 1: Основы виджетов (3-4 дня)

**Цель:** Создание, редактирование, удаление виджетов

**Задачи:**
- [ ] Создать `WidgetManager` класс
- [ ] Реализовать `create()` - создание виджета
- [ ] Реализовать `delete()` - удаление виджета
- [ ] Базовая Konva визуализация (прямоугольник + текст)
- [ ] Привязать контекстное меню к ImageManager
- [ ] Тестирование: создать 5 виджетов на одно изображение
- [ ] Тестирование: удалить виджет

**Результат:**
- ✅ Виджеты создаются и отображаются
- ✅ Контекстное меню работает
- ✅ Виджеты удаляются

**Файлы:**
- `public/js/widget-manager.js` - новый
- `public/js/widget-types.js` - новый
- `public/js/image-manager.js` - обновить (меню)
- `public/js/context-menu.js` - новый (переиспользуемое меню)

---

### Итерация 2: Позиция, размер, выделение (3 дня)

**Цель:** Редактирование свойств виджетов

**Задачи:**
- [ ] Реализовать `updatePosition()` с граничными проверками
- [ ] Реализовать `updateSize()`
- [ ] Интеграция с SelectionManager (выбор виджета)
- [ ] Базовая панель свойств (позиция, размер)
- [ ] Панель свойств обновляется при выборе
- [ ] Тестирование: выбрать виджет → показать свойства → изменить значения
- [ ] Тестирование: перетянуть виджет в пределах изображения

**Результат:**
- ✅ Выделение виджета работает
- ✅ Панель свойств показывает данные
- ✅ Можно менять позицию и размер

**Файлы:**
- `public/js/properties-panel.js` - обновить (добавить секцию виджетов)
- `public/js/selection-manager.js` - обновить (поддержка виджетов)
- `public/js/widget-manager.js` - обновить (драг, граничные проверки)

---

### Итерация 3: Типизация и оформление (3 дня)

**Цель:** Разные типы виджетов с уникальным оформлением

**Задачи:**
- [ ] Реализовать все 6 типов виджетов
- [ ] Для каждого типа: размер по умолчанию, цвета, параметры
- [ ] Визуализация каждого типа в Konva
  - toggle: прямоугольник с переключателем
  - led: круг с цветом (вкл/выкл)
  - number-display: текст с числом + единица
  - text-display: текст
  - slider: горизонтальный ползунок
  - gauge: стрелочный индикатор
- [ ] Панель свойств: оформление (цвет, размер шрифта)
- [ ] Сохранение типа и стиля в объекте виджета
- [ ] Тестирование: для каждого типа создать, отредактировать стиль

**Результат:**
- ✅ Все 6 типов видны на экране
- ✅ Каждый тип имеет свой внешний вид
- ✅ Можно менять оформление

**Файлы:**
- `public/js/widget-types.js` - обновить (все 6 типов)
- `public/js/widget-manager.js` - обновить (рендеринг по типам)
- `public/js/properties-panel.js` - обновить (секция оформления)

---

### Итерация 4: Синхронизация с изображениями (2 дня)

**Цель:** Виджеты движутся и масштабируются с изображениями

**Задачи:**
- [ ] Реализовать `onImageMove()` - виджеты следуют за изображением
- [ ] Реализовать `onImageResize()` - виджеты масштабируются
- [ ] Сохранять относительные координаты (relativeX, relativeY)
- [ ] Интеграция с ImageManager
- [ ] Тестирование: переместить изображение → виджеты движутся
- [ ] Тестирование: ресайзить изображение → виджеты масштабируются

**Результат:**
- ✅ Виджеты синхронизированы с изображениями
- ✅ При движении/ресайзе изображения виджеты обновляются

**Файлы:**
- `public/js/widget-manager.js` - обновить (синхро с изображением)
- `public/js/image-manager.js` - обновить (триггеры для виджетов)

---

### Итерация 5: Привязка к устройствам (3 дня)

**Цель:** Связь виджетов с ID устройств

**Задачи:**
- [ ] Загрузить devices-registry.json с backend'а (mock)
- [ ] Реализовать `updateBinding()` в WidgetManager
- [ ] Панель свойств: выпадающий список устройств
- [ ] Отображение метаданных (единица, диапазон, описание)
- [ ] **БЕЗ реалтайм данных** - только статические метаданные
- [ ] Сохранение bindingId в объекте виджета
- [ ] Тестирование: выбрать виджет → привязать к устройству → видны метаданные
- [ ] Тестирование: привязать несколько виджетов к разным устройствам
- [ ] Тестирование: отвязать виджет ("не привязано")

**Результат:**
- ✅ Выпадающий список с устройствами
- ✅ Метаданные отображаются
- ✅ Привязка сохраняется

**Файлы:**
- `public/js/properties-panel.js` - обновить (выпадающий список, метаданные)
- `public/js/widget-manager.js` - обновить (bindingId)
- `public/js/ui-controller.js` - добавить загрузку devices-registry
- `backend/config/devices-registry.json` - создать (mock файл)

---

### Итерация 6: Интеграция и полировка (2 дня)

**Цель:** Все компоненты работают вместе, готово к следующей фазе

**Задачи:**
- [ ] Полное тестирование workflow'а:
  1. Добавить изображение
  2. ПКМ → Добавить виджет
  3. Выбрать тип
  4. Отредактировать позицию, размер, оформление
  5. Привязать к устройству
  6. Переместить/ресайзить изображение
  7. Удалить виджет
- [ ] Обработка edge cases:
  - Удаление изображения с виджетами
  - Выделение/снятие выделения
  - Неправильные значения в полях свойств
- [ ] Визуальная полировка (иконки, цвета, шрифты)
- [ ] Проверка памяти (нет утечек при удалении)
- [ ] Документирование кода

**Результат:**
- ✅ Система виджетов полностью функциональна
- ✅ Готово к разработке save/load
- ✅ Готово к backend интеграции

---

## 7. ТАБЛИЦА СТАТУСА

| Итерация | Фокус | Дни | Статус |
|----------|-------|-----|--------|
| 1 | Основы (CRUD) | 3-4 | ⬜ |
| 2 | Позиция, размер | 3 | ⬜ |
| 3 | Типы и стили | 3 | ⬜ |
| 4 | Синхро с изображениями | 2 | ⬜ |
| 5 | Привязка к устройствам | 3 | ⬜ |
| 6 | Интеграция и полировка | 2 | ⬜ |
| **ИТОГО** | **Виджеты готовы** | **16-17** | ⬜ |

---

## 8. ВАЖНЫЕ МОМЕНТЫ

### ✅ Запомнить

1. **БЕЗ реалтайма в редакторе** - только метаданные, не живые данные
2. **Контекстное меню** - правый клик по изображению, без обязательного выделения
3. **Автовыбор виджета** - после создания виджет автоматически выбирается
4. **Относительные координаты** - при ресайзе изображения виджеты масштабируются
5. **Слой виджетов** - всегда выше изображений, никогда за ними
6. **Граничные проверки** - виджеты не могут выйти за границы изображения
7. **Синхронизация** - при движении изображения виджеты движутся, при удалении - удаляются
8. **Опциональная привязка** - не все виджеты должны быть привязаны

### ❌ Избежать

- ❌ Сохранение в JSON на этой фазе
- ❌ Backend интеграция (MQTT, WebSocket)
- ❌ Реалтайм данные от датчиков
- ❌ Многоуровневые диалоги (один клик = одно действие)
- ❌ Виджеты выходят за границы изображения
- ❌ Дублирование кода вместо переиспользования компонентов
- ❌ Отсутствие граничных проверок

---

## 9. TESTING CHECKLIST

### Итерация 1
- [ ] Создать 5 виджетов → все видны
- [ ] Удалить виджет → пропал
- [ ] Контекстное меню появляется при ПКМ на изображение
- [ ] Выбрать тип → виджет создан

### Итерация 2
- [ ] Выбрать виджет → панель свойств показывает данные
- [ ] Снять выделение → панель пуста
- [ ] Отредактировать позицию → виджет переместился
- [ ] Отредактировать размер → виджет изменил размер
- [ ] Перетянуть за границу → остановился на границе

### Итерация 3
- [ ] Для каждого типа проверить визуализацию
- [ ] Изменить цвет, шрифт → обновилось на экране
- [ ] Разные типы имеют разные параметры в свойствах

### Итерация 4
- [ ] Переместить изображение → виджеты движутся вместе
- [ ] Ресайзить изображение → виджеты масштабируются
- [ ] Удалить изображение → виджеты исчезают

### Итерация 5
- [ ] Выпадающий список показывает все устройства
- [ ] Выбрать устройство → метаданные появились
- [ ] Снять привязку → метаданные исчезли
- [ ] Разные виджеты привязаны к разным устройствам

### Итерация 6
- [ ] Full workflow test (см. выше)
- [ ] Нет консольных ошибок
- [ ] Нет утечек памяти при удалении
- [ ] UI отзывчив, нет лагов

---

## 10. КЛЮЧЕВЫЕ ФАЙЛЫ ДЛЯ ПРАВОК

```
public/js/
├── widget-manager.js          ← НОВЫЙ (ядро)
├── widget-types.js            ← НОВЫЙ (типы)
├── context-menu.js            ← НОВЫЙ (переиспользуемое меню)
├── image-manager.js           ← ПРАВИТЬ (привязать меню)
├── selection-manager.js       ← ПРАВИТЬ (поддержка виджетов)
├── properties-panel.js        ← ПРАВИТЬ (добавить секцию виджетов)
└── ui-controller.js           ← ПРАВИТЬ (инициализация)

backend/config/
├── devices-registry.json      ← НОВЫЙ (mock для этапа)
```

---

## NEXT STEPS

После завершения всех 6 итераций:

1. **Фаза 2: Save/Load** - сохранение schema.json + bindings.json
2. **Фаза 3: Backend** - MQTT, WebSocket, API
3. **Фаза 4: Dashboard** - оператор видит live данные

Виджеты готовы как основа для системы.
