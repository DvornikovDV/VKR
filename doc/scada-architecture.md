# SCADA Система: Архитектура от редактора к Dashboard

**Версия**: 1.0  
**Дата**: 05.01.2026  
**Статус**: Design Document  

---

## 🎯 Обзор

Этот документ описывает полную архитектуру IoT SCADA системы от создания схемы в редакторе до мониторинга оператором на Dashboard.

### Ключевое преимущество

**Один оператор может одновременно мониторить ВСЕ схемы системы:**

```
Оператор видит в Dashboard:
├── Теплица №1 (схема greenhouse_01.json)
├── Теплица №2 (схема greenhouse_02.json)
├── Водоснабжение (схема water_supply.json)
├── Электроснабжение (схема power_distribution.json)
└── ...

Все с live данными в реальном времени
```

Вместо Rapid SCADA (один оператор ↔ одна система), здесь:
**Один оператор ↔ Множество независимых систем**

---

## 🏗️ Трёхуровневая архитектура

### Уровень 1: JSON Схема (сохраняется в редакторе)

**Файл**: `schema.json`  
**Создатель**: Инженер в редакторе  
**Содержит**: Структуру системы (изображения, линии, элементы)  

```json
{
  "id": "schema_greenhouse_01",
  "name": "Теплица №1",
  "version": "1.0",
  "createdAt": "2026-01-05T00:00:00Z",
  "images": [
    {
      "id": "img_pump_1",
      "src": "data:image/png;base64,...",
      "x": 100,
      "y": 200,
      "width": 80,
      "height": 100,
      "name": "Помпа"
    }
  ],
  "connections": [
    {
      "id": "conn_1",
      "fromPin": { "imageId": "img_pump_1", "side": "right", "offset": 0.5 },
      "toPin": { "imageId": "img_motor_1", "side": "left", "offset": 0.5 },
      "points": [{"x": 150, "y": 250}, {"x": 200, "y": 250}]
    }
  ],
  "interactiveElements": [
    {
      "id": "elem_temp_display_1",
      "type": "sensor-display",
      "x": 150,
      "y": 50,
      "imageId": "img_pump_1",
      "bindingId": null
    },
    {
      "id": "elem_pump_switch_1",
      "type": "switch",
      "x": 120,
      "y": 320,
      "imageId": "img_pump_1",
      "bindingId": null
    }
  ]
}
```

**Важно:**
- Полностью независима от данных
- Версионируется в git
- Переносится между системами как есть

---

### Уровень 2: Конфигурация привязок (карта тегов)

**Файл**: `bindings.json`  
**Создатель**: Инженер в редакторе (кнопка "Привязать к тегу")  
**Содержит**: Связь элементов схемы с тегами оборудования  

```json
{
  "schemaId": "schema_greenhouse_01",
  "schemaVersion": "1.0",
  "createdAt": "2026-01-05T00:10:00Z",
  "bindings": [
    {
      "elementId": "elem_temp_display_1",
      "tagId": "greenhouse_01.sensors.temperature",
      "tagName": "Теплица 1 - Температура",
      "type": "sensor",
      "format": "decimal",
      "decimals": 1,
      "unit": "°C",
      "minValue": -10,
      "maxValue": 50,
      "alertAbove": 35,
      "alertBelow": 5
    },
    {
      "elementId": "elem_humidity_display_1",
      "tagId": "greenhouse_01.sensors.humidity",
      "tagName": "Теплица 1 - Влажность",
      "type": "sensor",
      "format": "decimal",
      "decimals": 1,
      "unit": "%",
      "minValue": 0,
      "maxValue": 100,
      "alertAbove": 90,
      "alertBelow": 20
    },
    {
      "elementId": "elem_pump_switch_1",
      "tagId": "greenhouse_01.devices.pump",
      "tagName": "Теплица 1 - Помпа",
      "type": "switch",
      "onValue": 1,
      "offValue": 0,
      "commandTopic": "greenhouse_01/pump/cmd"
    }
  ]
}
```

**Важно:**
- Отдельна от схемы (можно менять привязки без редитирования)
- Содержит метаданные (единицы, пределы, алерты)
- Легко менять без перерисовки схемы

---

### Уровень 3: Runtime данные (обновляются в реальном времени)

**Источник**: MQTT датчики / Устройства  
**Формат**: Поток обновлений через WebSocket  
**Задержка**: ~100-500ms  

```json
{
  "timestamp": "2026-01-05T00:12:00.123Z",
  "tags": {
    "greenhouse_01.sensors.temperature": 23.5,
    "greenhouse_01.sensors.humidity": 65.2,
    "greenhouse_01.sensors.light": 450,
    "greenhouse_01.devices.pump": 1,
    "greenhouse_02.sensors.temperature": 21.0,
    "greenhouse_02.sensors.humidity": 70.5,
    "water_supply.sensors.level": 85.0
  }
}
```

---

## 📊 Поток данных

```
┌──────────────────────────────────────────────────────────┐
│                    РЕДАКТОР (Инженер)                   │
│                                                          │
│  1. Рисует схему (изображения, линии, точки)            │
│  2. Добавляет интерактивные элементы (датчики, кнопки)  │
│  3. Привязывает элементы к тегам оборудования           │
│  4. Экспортирует: schema.json + bindings.json           │
│                                                          │
└──────────────────────┬──────────────────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        ↓                             ↓
   schema.json                  bindings.json
   (структура)                  (карта тегов)
        │                             │
        └──────────────┬──────────────┘
                       ↓
        ┌──────────────────────────┐
        │  DATABASE / STORAGE      │
        │  (хранилище схем)        │
        └──────────────┬───────────┘
                       ↓ (Admin загружает схему)
┌──────────────────────────────────────────────────────────┐
│               BACKEND (Node.js сервер)                  │
│                                                          │
│  1. Загружает schema.json + bindings.json               │
│  2. Парсит bindings → список тегов для подписки         │
│  3. Подписывается на все теги в MQTT брокере           │
│  4. Отправляет live данные в Dashboard через WebSocket │
│  5. Получает команды от Dashboard                       │
│  6. Отправляет команды обратно в MQTT                  │
│                                                          │
└──────────────────┬──────────────────┬───────────────────┘
                   │                  │
            WebSocket             MQTT pub/sub
                   │                  │
                   ↓                  ↓
┌──────────────────────────┐  ┌──────────────────────────────┐
│   DASHBOARD (Оператор)   │  │    MQTT Broker + Датчики     │
│                          │  │                              │
│  • Видит ВСЕ схемы      │  │  - MQTT pub/sub              │
│  • Live значения        │  │  - Raspberry Pi              │
│  • Управление switch    │  │  - Arduino                   │
│                          │  │  - PLC                       │
│  ВСЁ НА ОДНОМ ЭКРАНЕ!   │  │  - Реальные датчики          │
│                          │  │                              │
└──────────────────────────┘  └──────────────────────────────┘
```

---

## 🌟 Ключевое преимущество: Множественные схемы

### Scenario: Оператор на ферме

```
Время 12:00
Оператор открывает Dashboard

├─ Теплица 1 (greenhouse_01.json)
│  ├─ Температура: 23.5°C ✅
│  ├─ Влажность: 65% ✅
│  └─ Помпа: [OFF]
│
├─ Теплица 2 (greenhouse_02.json)
│  ├─ Температура: 28.2°C ⚠️ (высокая!)
│  ├─ Влажность: 72% ✅
│  └─ Помпа: [ON]
│
├─ Водоснабжение (water_supply.json)
│  ├─ Уровень резервуара: 85% ✅
│  ├─ Давление: 2.5 bar ✅
│  └─ Клапан: [OPEN]
│
└─ Электроснабжение (power_dist.json)
   ├─ Напряжение: 230V ✅
   ├─ Ток: 15A ✅
   └─ Генератор: [AUTO]

Оператор видит ВСЁ на ОДНОМ экране!
Одна схема засехла → оператор сразу видит и может действовать
```

### Сравнение подходов

| Аспект | Rapid SCADA | Ваше решение |
|--------|----------|---------------|
| **Одна система** | 1 оператор видит 1 схему | ✅ 1 оператор видит 1 схему |
| **Две системы** | 2 окна браузера / 2 планшета | ✅ 1 окно браузера - обе схемы |
| **5 систем** | 5 окон / 5 мониторов | ✅ 1 экран - все 5 схем |
| **Переключение** | Переключение между вкладками | ✅ Все видны одновременно |
| **Масштабируемость** | Нужна платформа побольше | ✅ Масштабируется на браузер |
| **Затраты** | Дорогой hardware | ✅ Любой браузер, любой device |

---

## 💻 Реализация

### Frontend: Редактор (ваш текущий код)

**Новые компоненты:**

```
public/
├── bindings-panel.js        (новый) - панель привязки элементов к тегам
├── tag-selector.js          (новый) - диалог выбора тега
└── ui-controller.js         (обновить) - добавить кнопку привязки
```

**Функциональность:**
1. Правый клик на интерактивный элемент → "Привязать к тегу"
2. Открывается диалог с полем ввода tag ID
3. При сохранении добавляется в `bindings.json`
4. При экспорте сохраняются оба файла

---

### Backend: Node.js сервер

**Новые компоненты:**

```
backend/
├── schema-loader.js         (новый) - загрузка схем и привязок
├── mqtt-client.js           (новый) - работа с MQTT брокером
├── ws-server.js             (новый) - WebSocket для Dashboard
├── tag-cache.js             (новый) - кэш текущих значений тегов
└── server.js                (обновить) - точка входа
```

**Функциональность:**
1. При запуске: загружает ВСЕ схемы из папки
2. Для каждой схемы: парсит bindings → подписывается на теги в MQTT
3. На каждое обновление датчика: отправляет в Dashboard
4. На команду от оператора: отправляет в MQTT

---

### Frontend: Dashboard (новый)

**Новые компоненты:**

```
dashboard/
├── index.html               - точка входа
├── dashboard-renderer.js    - отрисовка всех схем
├── ws-client.js             - подключение к WebSocket
├── schema-viewer.js         - просмотр одной схемы
└── styles.css               - CSS для Dashboard
```

**Функциональность:**
1. Подключается к WebSocket сервер
2. Получает список ВСЕ доступных схем
3. Рендирует их все в одном окне (grid или tabs)
4. Обновляет live данные
5. Отправляет команды при клике

---

## 📋 JSON Структуры

### Schema.json

```json
{
  "id": "schema_<system_id>",
  "name": "Человеческое имя",
  "version": "1.0",
  "createdAt": "ISO 8601 timestamp",
  "images": [{ id, src, x, y, width, height }],
  "connections": [{ id, fromPin, toPin, points }],
  "interactiveElements": [{ id, type, x, y, imageId, bindingId }]
}
```

### Bindings.json

```json
{
  "schemaId": "schema_<system_id>",
  "schemaVersion": "1.0",
  "bindings": [
    {
      "elementId": "elem_...",
      "tagId": "system.category.name",
      "tagName": "Человеческое имя",
      "type": "sensor" | "switch",
      "... (метаданные)": "..."
    }
  ]
}
```

### MQTT Topics Convention

```
{system_id}/{category}/{device}/{property}

Примеры:
  greenhouse_01/sensors/temperature
  greenhouse_01/sensors/humidity
  greenhouse_01/devices/pump
  greenhouse_02/sensors/light
  water_supply/sensors/level
  power_dist/devices/generator
```

---

## 🔄 Жизненный цикл системы

### День 1: Инженер создаёт схему

```bash
1. Открывает редактор
2. Рисует схему теплицы
3. Добавляет элементы (датчики, кнопки)
4. Привязывает к тегам:
   elem_temp → greenhouse_01.sensors.temperature
   elem_pump → greenhouse_01.devices.pump
5. Экспортирует: schema.json + bindings.json
6. Загружает на сервер
```

### День 2: Admin развёртывает

```bash
Copy files to server:
  scp schema.json bindings.json admin@server:/schemas/greenhouse_01/

Run backend:
  node backend/server.js --schemas-dir /schemas

# Backend подписывается на MQTT теги из bindings.json
# WebSocket слушает на ws://server:8080
```

### День 3: Оператор мониторит

```bash
# Открывает Dashboard
  https://server:3000/dashboard

# Видит все схемы системы в реальном времени
# Кликает по кнопке → command → MQTT → device
```

---

## 🎯 Миграция с текущей архитектуры

### Что уже есть

```
✅ 9 менеджеров (canvas, image, connection, point, selection, properties, file, diagram-element, connection-point-manager)
✅ Модульная архитектура
✅ JSON сохранение схемы
✅ UIController координатор
```

### Что добавить (по порядку)

```
1️⃣ BindingsPanel + TagSelector (2-3 дня)
   - Правый клик на элемент
   - Диалог привязки
   - Сохранение bindings.json

2️⃣ Backend: SchemaLoader + MQTTClient (2-3 дня)
   - Загрузка схем
   - Подписка на теги
   - WebSocket сервер

3️⃣ Dashboard: Renderer (2-3 дня)
   - WebSocket клиент
   - Рендер схем
   - Live обновления

4️⃣ Интеграция + тестирование (1-2 дня)
   - End-to-end flow
   - Mock данные
   - Demo "умная теплица"
```

**Итого: 1.5-2 недели до MVP**

---

## 💡 Расширяемость

### Будущие фичи

1. **Алерты и уведомления**
   ```json
   {
     "alertAbove": 35,
     "alertBelow": 5,
     "notification": "email",
     "recipients": ["engineer@farm.ru"]
   }
   ```

2. **История и тренды**
   ```
   - Сохранение всех значений в TimeSeries DB (InfluxDB)
   - График температуры за 24 часа
   - Экспорт CSV для анализа
   ```

3. **Автоматизация**
   ```json
   {
     "type": "automation",
     "condition": "temperature > 30",
     "action": "turn_on pump"
   }
   ```

4. **Multi-user с ролями**
   ```
   - Оператор: только просмотр
   - Инженер: создание схем
   - Admin: управление пользователями
   ```

5. **Облако и синхронизация**
   ```
   - Синхронизация схем между серверами
   - Backup автоматический
   - Версионирование в git
   ```

---

## 📊 Сравнение с конкурентами

| Фича | Rapid SCADA | Grafana | Ваше решение |
|------|----------|---------|---------------|
| **Редактор схем** | ⭐⭐⭐ | ❌ | ⭐⭐⭐⭐⭐ |
| **Drag-drop UI** | ⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Multi-schema dashboard** | ⚠️ Сложно | ❌ | ✅ Встроено |
| **Lightweight** | ❌ Тяжёлый | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| **IoT-first** | ⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Работает на RPi** | ❌ | ⚠️ Медленно | ✅ |
| **Цена** | $$ | $$$ | $$ (open source) |

---

## ✅ Контрольный список внедрения

- [ ] Инженер создаёт схему в редакторе
- [ ] Экспортирует schema.json + bindings.json
- [ ] Admin загружает файлы на сервер
- [ ] Backend подписывается на MQTT теги
- [ ] Оператор видит Dashboard с live данными
- [ ] Оператор может управлять переключателями
- [ ] Система масштабируется на 5+ схем одновременно
- [ ] All работает в реальном времени (<500ms задержка)

---

## 🚀 Результат

**Полнофункциональная IoT SCADA система:**

```
Редактор          Backend           Dashboard
   ↓                 ↓                  ↓
Инженер      Администратор        Оператор

Создаёт        Развёртывает       Мониторит
схемы       ← конфигурирует  ←  системы
              Управляет
```

**Ключевой результат**: Один оператор может управлять всеми системами на одном экране.

---

**Документ готов к внедрению! 🎯**
